{"_type":"issue","id":"mayor-aie31.8","title":"fix(servicelb): pin the eBPF maps so a loader restart keeps flow state","description":"Route every map through EbpfLoader::map_pin_path so a loader restart reuses the\npinned maps instead of creating empty ones.\n\nToday only links and programs are pinned, at src/main.rs:336 and :363.\nEbpf::load creates a fresh, empty map set on every start, and attach_and_pin\nthen calls bpf_link_update to swap the new program onto the existing pinned\nlink. The dataplane keeps forwarding, but against empty conntrack tables, so\nevery established flow breaks. In a DaemonSet that happens on every image\nrollout, eviction, and OOM kill.\n\nai/extended-context/ebpf-lb-dataplane.md line 133 already promises the\nopposite: pinned under /sys/fs/bpf so restarts keep flow state.\n\nSecondary win: during the swap the old and new map sets coexist, so peak\nkernel memory is roughly double the steady-state 1.82 MiB. Reuse removes that\nspike.\n\naya 0.14 supports this directly. EbpfLoader::map_pin_path routes each map\nthrough MapData::create_pinned_by_name, which tries bpf_get_object on the pin\npath first and only creates a map when the pin is absent.\n\nDONE WHEN: a loader restart preserves conntrack entries, proved by a smoke\ntest that holds a connection open across a restart and fails if the entries\nare lost.\n\n","notes":"INVARIANT for Phase 5, from the 2026-09-05 review discussion: reconciliation (a Service/EndpointSlice change) is a live per-key write to VIP_MAP and POD_TARGETS via bpf_map_update_elem/delete_elem, per ai/extended-context/ebpf-lb-dataplane.md:132 (load programs once, write maps on change, idle). It is NOT a program reload and NOT a process restart, so it never by itself resets the FWD_FLOW/REV_FLOW LRU conntrack maps -- those are separate kernel map objects untouched by a config-map write. The only things that reset the LRU maps are reload or restart with fresh unpinned maps, which is exactly the hazard this bead fixes. A well-built Phase 5 controller must never reload on a config change. Corollary: do NOT wire map auto-resize into the reconcile loop, since resize needs create-new-and-swap, which WOULD reset conntrack unless migrated.","status":"closed","priority":1,"issue_type":"bug","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:10Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:53Z","closed_at":"2026-09-08T15:40:53Z","close_reason":"PR #1585 opened: pin eBPF maps via EbpfLoader::map_pin_path so loader restart reuses pinned maps and preserves FWD_FLOW/REV_FLOW conntrack; restart-preservation smoke test added and verified to fail on revert.","dependencies":[{"issue_id":"mayor-aie31.8","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:03Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.9","title":"fix(servicelb): drop rather than forward a REV_FLOW miss on uplink egress","description":"Change the uplink-egress reverse-flow miss path from TC_ACT_OK to a drop plus\na counter, once the packet has been positively identified as backend traffic.\n\nuplink_egress_return at servicelb-ebpf/src/main.rs:533 maps a failed REV_FLOW\nlookup to TC_ACT_OK. On an LRU eviction the backend Pod's raw reply then\nleaves the node unencapsulated, carrying a pod-CIDR source address onto the\nunderlay. That both stalls the connection and leaks a pod-source packet,\nwhich is worse than dropping it.\n\nThe forward decap path already treats its equivalent miss as fatal:\ngeneve_ingress maps a failed try_geneve_decap_return to TC_ACT_SHOT. The\negress path should be consistent.\n\nCare needed: try_uplink_egress_return returns None both for \"not our traffic\"\nand for \"reverse entry missing\", and today both collapse to TC_ACT_OK.\nNon-ServiceLB traffic must still pass untouched, so the two cases have to be\nseparated before a drop is safe. The POD_TARGETS membership gate bead\nprovides exactly that positive identification cheaply.\n\nDONE WHEN: an evicted reverse entry produces a drop and an incremented\ncounter, with a regression test that fails if the fallback returns to\nTC_ACT_OK for identified backend traffic, and unrelated egress traffic still\npasses.\n\n","status":"closed","priority":1,"issue_type":"bug","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:10Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:52Z","closed_at":"2026-09-08T15:40:52Z","close_reason":"PR #1624 opened","dependencies":[{"issue_id":"mayor-aie31.9","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:03Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.9","depends_on_id":"mayor-aie31.14","type":"blocks","created_at":"2026-09-09T00:41:29Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.11","title":"design(servicelb): flow-table admission control","description":"Decide how this dataplane admits a flow into the conntrack table before\nanyone picks a max_entries ceiling, because table size cannot fix a table\nthat 8192 packets can cycle.\n\nuplink_ingress inserts into FWD_FLOW at servicelb-ebpf/src/main.rs:238\nimmediately after the VIP_MAP match, with no TCP state check, no SYN gate,\nand no validation of any kind. Any packet that reaches a service address\ncreates an entry, so an off-path sender can evict all legitimate flow state\nwith 8192 packets from varying source ports, which is milliseconds of\ntraffic. Sizing against expected legitimate concurrency does not help.\n\nLRU never rejects, it evicts, so the failure is silent rather than loud.\nForward evictions drop return traffic; reverse evictions leak an\nunencapsulated reply. Both are covered by sibling beads.\n\nResearch and decide among:\n\n- Promote on evidence of bidirectionality. New flows enter a small\n  unconfirmed pool and move to the main table only when the return direction\n  is observed. Modelled on nf_conntrack's unreplied versus assured states.\n  Protocol-agnostic, so one mechanism covers TCP, UDP, and QUIC. This is the\n  recommended starting point if only one thing gets built.\n- QUIC specifics. RFC 9000 section 8.1.2's Retry token is a first-class\n  address-validation handshake. Better still, this dataplane already mints\n  the DCID, so minting it with an embedded MAC makes every later short-header\n  packet self-authenticating and prevents state creation outright. Note that\n  draft-ietf-quic-load-balancers was declined at doc line 108 on routing\n  grounds; the anti-flush property is a separate argument that was not\n  weighed. RFC 9000 section 14.1's 1200-byte Initial minimum also raises\n  flood cost roughly thirtyfold over a bare TCP SYN.\n- TCP. A SYN gate is cheap but weak, since setting the bit is free. SYN\n  cookies are the real analogue and need care in a load balancer that does\n  not terminate TCP.\n- UDP. No L4 signal exists at all: no flag, no handshake, no field\n  distinguishing a first packet from a mid-stream one. The available levers\n  are per-source-prefix rate limiting on state creation and per-service\n  opt-in.\n\nDONE WHEN: a written decision on the admission mechanism exists, and the\nmax_entries ceiling is settled as a consequence of it rather than guessed\nindependently.\n\n","notes":"DECLINED, with rationale, from the 2026-09-05 review discussion: runtime dynamic resize of the conntrack maps (scale max_entries up/down within a min/max range based on live traffic). Two independent reasons. (1) Economics: the table is ~120 bytes/entry (measured), so shrinking a tiny-cluster table from 8192 to 1024 entries saves ~860 KiB -- under 0.1% of a 1 GB node -- while the scale-DOWN direction a tiny cluster wants is the hardest to measure (silent LRU eviction, no live count). (2) Timing: a control loop reacts in seconds; a sudden burst (HN front page) saturates in milliseconds, so you cannot autoscale out of a burst -- by the time you detect+allocate+migrate+swap it has passed or already thrashed. Burst survival comes from graceful LRU degradation (never returns E2BIG, evicts LRU, self-heals on next packet, fixed footprint regardless of load -- cannot OOM from traffic) plus a pre-provisioned fixed ceiling plus admission control (this bead) to protect established flows from a flood of new ones. The legitimate-traffic burst and the malicious flush attack are the SAME mechanism seen from two sides. Right-sizing belongs at load time (discrete tiny/default/large profiles from node facts), informed by observability metrics (mayor-aie31.22), not a runtime swap. Corollary already on aie31.8: do not wire resize into the reconcile loop.\nFindings doc: ai/findings/2026-09-06-mayor-aie31.11-flow-table-admission-control.md (branch worker/agent-aea30cb1ec81c8e99, commit 6a6cdbba). Recommendation: adopt option (a) promote-on-bidirectionality (nf_conntrack unreplied/assured, observable at ingress because symmetric return puts both legs on one node) as the mandatory protocol-agnostic foundation; ceiling derived as a consequence -\u003e split FWD_FLOW into FWD_PENDING=2048 (only flood-exposed tier) + FWD_MAIN=8192 (unchanged but now justified). QUIC self-authenticating minted-DCID MAC (option b) conditional on g3lag choosing CID-based keying; reject TCP SYN-cookies (needs TCP termination). Couplings flagged: g3lag (anti-flush is a new argument for CID keying), aie31.21 (FWD_FLOW becomes affinity store; two maps must be designed jointly), aie31.20/aie31.17 (map substrate). Awaiting operator decision.\n## critical-reviewer findings — findings-doc — mayor-aie31.11\n\n**Verdict**: LGTM-with-suggestions\n\n**Confirmed findings** (must be true, evidence cited):\n- [MED] The core topology claim is TRUE: forward (`uplink_ingress`) and return (`geneve_ingress` VNI_RET) legs run on the same ingress node by construction, so the return-path `FWD_FLOW.get` at main.rs:514 genuinely is a bidirectionality proof. Evidence: `main.rs:246` inserts keyed on ingress-local tuple, `main.rs:431` (`RevFlowValue.ingress_node_ip = tkey.remote_ipv4`) captures the originating node, `main.rs:598` (`uplink_egress_return`) Geneve-redirects the reply back to that exact node. No fix needed — this confirms the doc's foundation is sound.\n- [MED] The DONE-WHEN ceiling table splits `FWD_PENDING`/`FWD_MAIN` for TCP/UDP but defines no PENDING tier for the QUIC CID map, leaving the Answer's \"closes...for TCP, UDP, and QUIC with one mechanism\" claim structurally unbacked for QUIC. Evidence: doc lines 197-201 (table has only \"Main (QUIC)\", no \"Pending (QUIC)\") vs. line 14 and line 122 (\"QUIC? Same — the first server response promotes the flow\"). Suggested fix: add a QUIC PENDING/MAIN split to the ceiling table or explicitly justify why QUIC's CID map doesn't need one.\n- [MED] Option (b)'s \"LB mints DCID with embedded MAC\" mechanism rests on the same \"LB-minted CID\" model that sibling finding mayor-g3lag (committed 12s earlier, same session, commit 78c738bf) explicitly refutes as unrealizable for a non-terminating dataplane under BOTH of g3lag's realizable keying paths — pass-through has no CID map, and QUIC-LB method 3 has the *backend* mint the CID, not the LB. Evidence: aie31.11 doc lines 152-161 (\"this dataplane already mints the DCID... If it mints it with an embedded keyed MAC\") vs. g3lag doc lines 222-225 (\"the anti-forgery property comes from the backend's encrypted server_id encoding, not an LB-minted MAC... not off a MAC the LB stamped\"). aie31.11's framing (\"if and only if mayor-g3lag chooses CID-based keying\") implies (b) becomes buildable as-written once g3lag picks CID keying; per g3lag it does not — it needs redesign to validate a backend-minted value, not describe minting one. Suggested fix: reframe (b) as \"validate backend-minted QUIC-LB server_id\" conditional on g3lag's method 3, not \"LB mints DCID+MAC\" conditional on \"CID-based keying.\"\n\n**Suspicions** (worth the maintainer's attention, but I did not confirm):\n- The non-atomic PENDING get→insert→delete promotion sequence is claimed benign under all interleavings. Why suspicious: claim is asserted, not demonstrated against concurrent-CPU BPF map semantics. To verify: trace a concrete two-CPU interleaving (forward re-insert racing a return-triggered promote/remove) against BPF map atomicity guarantees, or defer to implementation-time review.\n\n**Not reviewed** (out of scope for this deliverable's checklist):\n- netfilter `early_drop`/`IPS_ASSURED` internals — doc itself discloses these are from established knowledge, not a fetched source.\n- RFC 9000 §8.1.2/§14.1 text — not independently re-fetched; relied on general knowledge and g3lag's own overlapping RFC citations.\n\n**Meta**: reviewed at 2026-09-06T09:44:31Z, deliverable size (1 file / 285 lines), checklist items checked (5/5 findings-doc checklist items: citation quality, verdict soundness, follow-on beads, uncertainty language, file location).\n\nOverall: the mandatory recommendation (adopt promote-on-bidirectionality as the protocol-agnostic foundation) is sound, its topology premise is verified true against `main.rs`, and the fallback for QUIC under 4-tuple pass-through (option (a) + §14.1 size gate, no MAC) is real and matches g3lag's own conclusion — the operator can rely on that core recommendation. The two MED findings above affect only the DONE-WHEN table's QUIC completeness and the conditional/optional MAC layer, not the mandatory foundation.\nDECISION (operator 2026-09-07): ACCEPT option (a) promote-on-bidirectionality as the flow-table admission foundation -- 2-tier, new flows enter FWD_PENDING and promote to FWD_MAIN only when the return leg is observed (protects established flows from a spoofed flood). Protocol-agnostic; QUIC uses the same (a) since g3lag chose tuple-based, so option-b DCID-MAC is NOT built for the MVP. CONSTRAINT (operator): the map sizes (FWD_PENDING, FWD_MAIN, and any per-protocol tier) MUST be CONFIGURABLE by the userspace DaemonSet at load time -- NOT hard-coded consts in the eBPF object. Implementation pending under the aie31 build phases; bead stays open as impl-ready.","status":"closed","priority":1,"issue_type":"decision","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:51Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:51Z","closed_at":"2026-09-08T15:40:51Z","close_reason":"Implemented and PR opened: two-tier promote-on-bidirectionality flow admission (FWD_PENDING/FWD_MAIN split), configurable map sizes, VipBackend value type for aie31.21 forward-compat. PR https://github.com/litehop/u7s/pull/1611","dependencies":[{"issue_id":"mayor-aie31.11","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:04Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.11","depends_on_id":"mayor-aie31.22","type":"relates-to","created_at":"2026-09-09T00:41:37Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.10","title":"fix(servicelb): POD_TARGETS holds one port per pod, breaking multi-port Services","description":"POD_TARGETS maps a pod IP to a single u16, so any Pod with two distinct\ntarget ports is already mis-DNATed in shipped code.\n\nservicelb-ebpf/src/main.rs:130 declares HashMap\u003cu32, u16\u003e keyed on pod IP\nalone, with no port set and no protocol dimension. The forward decap reads it\nat line 345 to pick the DNAT target port. One value per pod cannot represent:\n\n- a multi-port Service, for example 80 to 8080 alongside 443 to 8443\n- a Pod backing two Services, for example app traffic plus a metrics endpoint\n- TCP and UDP on different ports\n\nHTTP/3 happens to survive this. A Service declares TCP 443 and UDP 443 as two\nport entries, because a Service port entry carries exactly one protocol, and\nboth point at the same target port. The map stores only the number, so one\nentry serves both. That is coincidence, not coverage.\n\nWire-format wrinkle: widening the map is not sufficient on its own. The\nforward Geneve option carries only the raw pod IP, per the settled decision\nat ai/extended-context/ebpf-lb-dataplane.md line 149, so the backend cannot\ndisambiguate two target ports for one pod. Either the option grows, or the\nbackend re-keys off what the packet still carries at decap time, namely the\nVIP, the VIP port, and the protocol. That information is present and\nsufficient, so the fix exists, but it is a design change rather than a\nwidening of the value type.\n\nBlocks Phase 5, mayor-9gr0n, for a plain multi-port Service.\n\nDONE WHEN: a Service with two target ports on one Pod routes both correctly,\ncovered by a test that fails against the current single-value map.\n\n","status":"closed","priority":1,"issue_type":"bug","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:50Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:52Z","closed_at":"2026-09-08T15:40:52Z","close_reason":"PR #1573 opened","dependencies":[{"issue_id":"mayor-aie31.10","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:04Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-waqhd","title":"net/servicelb: coexistence verification with kube-proxy and flannel (east-west untouched)","description":"Phase 6 of mayor-aie31 (ServiceLB implementation epic). Proves the\nconsequence docs/decisions/servicelb-ebpf-geneve-dataplane.md states but\ndoes not itself verify: kube-proxy stays unmodified for east-west\nClusterIP/NodePort traffic, and this dataplane owns north-south\nLoadBalancer VIP:PORT traffic only, with no double-processing between\nthe two and no flannel address-space collision.\n\nChecks to run against a controller-driven system (Phase 5, not static\nfixtures): the tc-bpf ingress classifier on the physical uplink never\nmatches ClusterIP/NodePort traffic, only LoadBalancer VIP:PORT; the VIP\naddress range sits outside flannel's pod-CIDR and Service-CIDR, and\ngeneve0 never touches flannel's vxlan device, confirmed live, not just\nby address-range inspection; the one legitimate contact point, backend\ndecap handing its DNAT'd packet to flannel's pod-CIDR routing for the\nlast hop, behaves correctly under concurrent east-west and north-south\ntraffic on the same node; kube-proxy's existing iptables/IPVS rules and\nthis dataplane's tc-bpf hooks do not fight over the same traffic class.\n\nDepends on Phase 5 (mayor-9gr0n): needs a controller-driven system with\nreal Service objects of both kinds (ClusterIP and LoadBalancer)\ncoexisting, not Phase 2's single static VIP fixture. Blocks Phase 7\n(conformance gating): conformance runs assume east-west correctness is\nalready proven, not concurrently discovered.\n\nRisk: MEDIUM. Mostly an integration-verification phase; the failure\nmode (a routing rule ordering bug that lets the two dataplanes double\nprocess or misclassify a packet) is a regression in EXISTING east-west\nbehavior, which the project's conformance suite already gates, raising\nthe cost of a miss.\n\nVerification: Lima cluster running flannel plus kube-proxy plus this\ndataplane together; existing ClusterIP/NodePort conformance-relevant\npaths unaffected, confirmed via the existing e2e-focus suite, while a\ntype=LoadBalancer Service concurrently exercises the new path.\n\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:19Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:19Z","dependencies":[{"issue_id":"mayor-waqhd","depends_on_id":"mayor-9gr0n","type":"blocks","created_at":"2026-09-09T00:41:31Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-waqhd","depends_on_id":"mayor-aie31","type":"discovered-from","created_at":"2026-09-09T00:41:36Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-tavxy","title":"servicelb-ebpf: inbound Geneve-decap delivery must validate target pod against local serving-set to bound cross-node drift to a safe drop, not a misdelivery","description":"try_geneve_decap_forward (crates/servicelb/servicelb-ebpf/src/main.rs:331-471), the\nbackend/delivery node's inbound decap path, trusts the pod_ip stamped into the Geneve\noption by the ingress node and DNATs to it unconditionally. Its only membership check,\nTARGET_PORTS.get(VipKey{vip_ip,vip_port,proto}) (main.rs:361-368), is keyed on the front\ntuple only -- it confirms this node hosts SOME backend for the front, never that the\nspecific pod_ip in the packet is a pod CURRENTLY serving that front on this node. There\nis no analogue of aie31.14's egress POD_TARGETS-membership gate on this inbound path.\n\nWhy it matters (mayor-1dwwz cross-node convergence review of aie31.21): each node's\nuserspace controller processes the same EndpointSlice event at a different wall-clock\ntime (uplink latency), so per-node eBPF state can drift apart for multiple seconds. A\nlagging ingress node can replay a stale FWD_FLOW pin carrying a departed pod's IP. If\nthat IP is reused by a new pod on the backend node before the lagging ingress evicts its\npin, today's code delivers the packet to the NEW pod -- misdelivery to a live, unrelated\nworkload, not a bounded drop/hang. The drift window directly widens the exposure.\n\nFix direction: before the DNAT+deliver in try_geneve_decap_forward, gate on a node-local\nmap keyed on (front tuple, pod_ip) -\u003e current-membership, populated by this node's OWN\nEndpointSlice watch (lag-free for its own local pods), and TC_ACT_SHOT if pod_ip is not\na current member. This makes the delivery node -- not the ingress node -- the\nauthoritative consistency point: ingress-side drift then degrades to a drop at the\nboundary, never a cross-pod leak.\n\nDONE WHEN: try_geneve_decap_forward rejects a packet whose Geneve pod_ip is not in the\nbackend node's current local serving-set for that front, with a regression test that\nfails if the check is removed/bypassed, and live flows to still-serving pods are\nunaffected.\n\nCross-refs: aie31.21 (crates/servicelb affinity+eviction findings doc, this bead is a\nfollow-on to its cross-node-convergence section) and aie31.14 (the egress-side\nPOD_TARGETS membership gate this inbound gate mirrors, on the opposite hook).\n","notes":"HELD 2026-09-07 (operator decision): #1615's gate is correct+fail-closed but INERT (VIP_MAP is cluster-wide-identical from a static fixture until 9gr0n adds per-node watch) AND its single-value equality check must be rewritten as set-membership once 9gr0n makes VIP_MAP per-node+multi-endpoint. PR #1615 CLOSED (not merged); branch worker/agent-aca0e7737340044e4 preserved as reference impl (fail-closed gate_inbound_pod + fail-on-revert tests). Implement as part of / right after 9gr0n. Blocked-by mayor-9gr0n.\nReference impl PINNED durably (2026-09-08): PR #1615 (CLOSED) commit f6e58aec0c8c8157c8edbbb3da8342e678dd4ebc, retrievable via 'gh pr diff 1615' or 'git fetch origin refs/pull/1615/head'. Origin branch was auto-deleted on PR close; local branch worker/agent-aca0e7737340044e4 force-deleted to stop it recurring as a worktree-hygiene STEP-D anomaly. The gate_inbound_pod fail-closed structure + fail-on-revert tests live at that commit; re-implement for multi-endpoint set-membership when 9gr0n lands.","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:17Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:17Z","dependencies":[{"issue_id":"mayor-tavxy","depends_on_id":"mayor-1dwwz","type":"discovered-from","created_at":"2026-09-09T00:41:35Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-tavxy","depends_on_id":"mayor-9gr0n","type":"blocks","created_at":"2026-09-09T00:41:31Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-tavxy","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:41:25Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-tavxy","depends_on_id":"mayor-aie31.14","type":"relates-to","created_at":"2026-09-09T00:41:38Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-lrbvo","title":"net/servicelb: prototype go/no-go gates -- real-fleet return-path + RSS measurement","description":"## CURRENT STATE (2026-09-08) — distilled; full journaling in notes below\n\nPhase-4 go/no-go, 3 gates:\n- GATE 2 (RSS/memory): effectively PASS. ~1.82 MiB/node conntrack maps (LRU_HASH preallocates 8192 entries -\u003e constant cost, ~0.18% of 1 GiB). Lima-measured; sampler tooling landed (tk4ku/aie31.6).\n- GATE 3 (monitorability): satisfied by the landed bpftool/RSS sampler.\n- GATE 1 (cross-node symmetric-return round-trip): IN PROGRESS, NOT operator-blocked for the MVP. Tiered scope: TIER 1 = 2 Lima VMs over WireGuard (local, dual-stack) = the MVP; TIER 2 = 2 public-IP cloud nodes (operator provisioning); TIER 3 = 3-node incl NAT-home + IPv6-only (deferred). tc-bpf attach to an L3-only WireGuard iface WORKS (verifier accepts, all 3 progs attach). Round-trip not yet proven; two LOCAL blockers (neither needs external infra): (1) aie31.5 -- servicelb-ebpf hard-codes ETH_HLEN=14 and no-ops on an L3 wg uplink (code fix, prerequisite); (2) wg data-plane session would not come up on the Lima guest (wg set/show OK, no UDP listen socket) -- NOT the kernel (operator runs 4 physical nodes on that exact kernel over Tailscale), a local netns/Lima-config issue to diagnose with evidence.\n\nNEXT STEP (highest value, operator-directed 2026-09-08): build+validate the Tier-1 2-Lima-over-WireGuard multi-node setup (aie31.7, incorporating the aie31.5 fix), proving the cross-node round-trip -\u003e closes Gate 1 -\u003e unblocks Phase 5 (9gr0n).\n\n## ORIGINAL DEFINITION\nPhase 4 of mayor-aie31: validation checkpoint (not new mechanism) running the 3 prototype go/no-go gates from ai/extended-context/ebpf-lb-dataplane.md before scaling investment. Depends on Phase 3 (pa0ze, closed); blocks Phase 5 (9gr0n). Verification is live-measurement only (bpftool + OS RSS + an actual client-\u003eVIP-\u003ecross-node-backend-\u003eclient round trip).\n","notes":"Clarification spike verdict (read-only, no VM run): PARTIAL -- 2 disjoint Lima networks do NOT fully satisfy gate 1 as-is, but the plan's stated reason overstates the blocker.\n\n(a) Disjoint L3 subnets: Lima CAN provide this. user-v2-workers-a vs user-v2-workers-b are genuinely separate DHCP-assigned subnets / separate L2 segments (scripts/conformance/lima-start.sh comments L92-98, L986-987: 'Separate Lima networks are separate L2 segments with no path between them'). The plan's justification for 'Lima cannot substitute' (ebpf-lb-dataplane.md gate 1: 'none of the disjoint-subnet/NAT conditions...occur on a single-VM L2') is scoped to ONE VM's flat network -- it does not address, and does not rule out, 2 VMs on 2 disjoint Lima networks. That specific justification is imprecise as written.\n\n(b) NAT/asymmetric path between them: HARD BLOCKER today, not fundamental. add-node.sh/lima-start.sh's inter-node route loop explicitly hard-fails cross-network pairing: 'no L2 path exists between separate Lima networks, refusing to program an unreachable route' (lima-start.sh ~L994-997). Zero built-in connectivity exists between user-v2-workers-a and -b right now. A manual NAT/relay bridging the two networks' daemons (both run on the same Mac host) is theoretically constructible but does not exist in this codebase and is unbuilt/unverified.\n\n(c) Even if built, FIDELITY LIMITATION: a hand-rolled Mac-hosted relay only proves the Geneve mechanism crosses SOME NAT hop -- not the real Linode/Scaleway/home-router+Tailscale fleet's provider-specific behavior (real anti-spoof/uRPF, real NAT-traversal under real NAT types, real cross-provider latency/MTU) that the plan's open question is actually about (real-FLEET sign-off, not just a mechanism smoke test).\n\nGate 2 (RSS = resident memory, NOT NIC hardware -- no RSS/NIC-queue requirement exists anywhere in this Phase) needs NO real fleet at all: already Lima-provable, and precedent proves it -- loxilb's memory floor was measured on exactly a 1GB/1vCPU Lima VM (cni-svclb-landscape.md). Plan doesn't claim gate 2 needs real infra; only gate 1 is flagged Lima-insufficient, correctly for the reasons in (c) but overstated for (a)/(b).\n\nMinimal next step: a live 2-VM test (user-v2-workers-a + -b, with or without a manual NAT relay) was NOT run in this spike -- next-session follow-up, not guessed here. Even if it passes, real-fleet gate 1 sign-off per (c) still stands as a separate operational requirement.\nCLARIFICATION 2026-09-04 operator-confirmed: Phase 4 deploys the eBPF WITHOUT the Phase-5 DaemonSet -- which by construction cannot exist yet, since this bead BLOCKS Phase 5 mayor-9gr0n. Deployment for the go/no-go gate = run the Phase-1 loader binary from mayor-g6u8s build/load wiring, already CLOSED, MANUALLY on each real fleet node via ssh, populating STATIC fixture map contents Phase-2 style and sized per Phase 3. No dynamic Service/EndpointSlice watching and no managed per-node lifecycle are needed for a hand-run prototype measurement -- those are exactly what the Phase-5 DaemonSet mayor-9gr0n adds for production, and are useless before this gate proves the mechanism survives real-fleet NAT and RSS. So there is NO circular dependency: loading is available from Phase 1 onward, independent of the DaemonSet. The open operational dependency is real-fleet ACCESS -- ssh to the Linode/Scaleway/home-NAT nodes -- not a missing deployment mechanism.\nPHASE-4 GATE-1 PREREQS 2026-09-04 researcher-confirmed (operator asked, preparing fleet ahead). REACHABILITY: both nodes need BIDIRECTIONAL IP reachability to each other, but NOT public routability -- a Tailscale/WireGuard overlay supplies the path. One-way NAT-initiate-only does NOT suffice: symmetric-return requires the backend to send the return Geneve packet back to the ingress node. Docs: servicelb-symmetric-geneve-return.md L16-52, ebpf-lb-dataplane.md L16 and L45-71. IP FAMILY: IPv6-only nodes fully supported for BOTH ingress and backend -- Geneve underlay supports IPv6 per RFC 8926, aya tc-bpf via bpf_skb_set_tunnel_key, conntrack key already sized for IPv6 at 37 bytes; Scaleway IPv6-only is an explicit target. A fully IPv6-only node is acceptable. TODO when g7jh2/#1557 lands: verify the Phase-2 packet code has no AF_INET-only assumption. LIMA AS ONE NODE: a mixed 1-Lima + 1-real-cloud-over-Tailscale topology is MECHANISM-sufficient ONLY -- it loses real-fleet fidelity on uRPF/anti-spoof, real NAT traversal, and cross-provider latency/MTU -- so fine for early mechanism validation but NOT for operator production sign-off, which gate-1 requires on REAL disjoint subnets. MINIMAL FLEET: 3 real nodes over Tailscale ideal -- Linode dual-stack, Scaleway IPv6-only, home-NAT dual-stack; a 2-real minimum Linode+home-NAT loses IPv6-only and cross-provider coverage. Per-node prereqs: u7s control plane + kubelet + Flannel; manual-ssh Phase-1 loader deploy from g6u8s; Phase-3 pa0ze map sizing must land first; Tailscale client per node with cluster reachable over the tailscale0 100.64.0.0/10 interface.\nPHASE-4 SCOPE REVISED (operator 2026-09-04) -- supersedes the earlier PHASE-4 GATE-1 PREREQS note's IPv6-only-required + 3-node-ideal framing for MVP purposes. (1) IPv6-only is DESCOPED from the gate-1 MVP blocker: IPv6 was never tested at all, so gating the core symmetric-return mechanism on an untested IP-family axis risks an IPv6-specific issue masquerading as a mechanism failure. Prove the mechanism on dual-stack/IPv4 FIRST; IPv6-only becomes a SEPARATE follow-on, not a gate-1 blocker. (2) Tailscale/WireGuard is NOT a blanket requirement -- only needed if a node lacks a publicly routable IP (the home-NAT case). Two public-IP nodes peer directly, no overlay. (3) Operator insight: if an overlay is in the path anyway, two LIMA VMs over WireGuard/Tailscale test the same cross-node symmetric-return path as cloud nodes -- so the MECHANISM MVP needs NO external fleet access, removing the operational blocker. REVISED TIERING: TIER 1 (MVP, dispatchable, no external access): 2 Lima VMs over WireGuard, dual-stack, prove symmetric-return round-trip cross-node. CAVEAT to verify while scoping tier 1: our tc-bpf Geneve path must attach cleanly to a WireGuard tun interface (no L2 header, unlike a real NIC). TIER 2 (real-internet fidelity, needs 2 public-IP cloud nodes = operator provisioning): Geneve over real paths -- real MTU/PMTU, provider uRPF/anti-spoof, real NAT. TIER 3 (full fidelity, deferred): the original 3-node incl. NAT-home + IPv6-only Scaleway. Still downstream of pa0ze (map sizing). Next step (operator to greenlight): a tier-1 Lima-over-WireGuard spike bead.\nOPERATOR DIRECTIVE 2026-09-04: greenlit trying TIER 1 (WireGuard-over-Lima) FIRST. Fallback: if WireGuard-over-Lima proves impossible, operator provisions VPS nodes -- and then Tailscale is needed to avoid the reciprocal-public-routability requirement between the VPS. Feasibility spike dispatched (plumbing-only, independent of pa0ze) -- verdict recorded here as POSSIBLE/IMPOSSIBLE.\nWIREGUARD-OVER-LIMA SPIKE VERDICT: IMPOSSIBLE (as currently coded) -- mechanically POSSIBLE, functionally blocked. wg-tun tc-bpf attach: PASS -- created a real wg-type netdev (link/none, ARPHRD_NONE, no L2) in an isolated netns on lima-node-smoke and pointed the actual servicelb-ebpf loader's --uplink-iface at it; verifier accepted uplink_ingress/uplink_egress_return/geneve_ingress and all 3 attached as TCX links (confirmed via bpftool net list: 'wg-node0(2) tcx/ingress uplink_ingress', 'wg-node0(2) tcx/egress uplink_egress_return', 'geneve-wgspike(3) tcx/ingress geneve_ingress') -- kernel-level attach to a no-L2 device is not the blocker. Cross-wg round-trip: NOT COMPLETED -- couldn't get an actual WireGuard data-plane session up on this guest's kernel (7.0.0-30-generic): 'wg set \u003cif\u003e listen-port N ...' returns success and 'wg show' reports the config back, but no UDP socket for that port ever appears in /proc/net/udp or /proc/net/udp6 in either root or a custom netns, and tcpdump on the underlay veth captured zero packets during a ping across the tunnel -- looks like an environment/kernel quirk unrelated to the eBPF question, not investigated further given budget. Specific blocker (found by reading crates/servicelb/servicelb-ebpf/src/main.rs, not just inferred): uplink_ingress and try_uplink_egress_return hard-code ETH_HLEN=14 and unconditionally read the EtherType at ctx.load::\u003cu16\u003e(12) then index the IPv4 header at ETH_HLEN-relative offsets -- this assumes every uplink-ingress packet carries a real 14-byte Ethernet header. A WireGuard interface is L3-only (confirmed empirically: 'ip -d link show wg-node0' -\u003e link/none) and delivers packets with NO such header, so on a real wg uplink ctx.load::\u003cu16\u003e(12) reads into the middle of the real IP header (bytes 12-13 = the first two bytes of the source IP), essentially never equals 0x0800, and uplink_ingress falls through to TC_ACT_OK on every single packet -- the VIP redirect logic never fires, so nothing is intercepted; the dataplane would silently no-op on a wg uplink today, not crash or reject at load time. One-line: this does NOT clear tier-1 MVP as-is for a WireGuard uplink, but it is a small, well-understood eBPF code fix (make the L2-header skip conditional/absent for L3-only uplinks), not a kernel/infra limitation -- the operator does not need to provision VPS nodes to prove the mechanism; the mayor should route a code fix to whoever owns servicelb-ebpf (currently pa0ze, per this spike's brief) before the next smoke run, then re-run this same fail-fast attach check to confirm the fixed parse path also attaches+works on wg. Guest fixture fully torn down (netns/veth/wg devices/bpf pins/scratch files removed, wireguard module unloaded, lima-node-smoke's original 7-interface topology and bpf prog list confirmed unchanged post-cleanup).\nWIREGUARD-OVER-LIMA SPIKE VERDICT (2026-09-04): NOT impossible, NO VPS needed. tc-bpf ATTACH to an L3-only/no-L2 WireGuard interface WORKS (verifier accepts, all 3 progs attach as TCX, confirmed via bpftool) -- the main risk is cleared. Round-trip NOT yet proven, blocked by TWO LOCAL issues (neither needs external infra): (1) servicelb-ebpf assumes an Ethernet L2 header (ETH_HLEN=14) -\u003e silently no-ops on the L3 wg uplink -- concrete code fix, filed as a new aie31 child (shared servicelb-ebpf surface; dispatch AFTER #1567 merges); (2) a WireGuard DATA-PLANE session could not be established on lima-node-smoke kernel 7.0.0-30-generic (wg set/show OK but no UDP listen socket appears, tcpdump saw zero underlay packets) -- env/kernel quirk to resolve while building tier-1 (try a different Lima kernel/image, or userspace wireguard-go). Once both fixed, re-run the harness round-trip over wg to confirm tier-1 MVP. NET: tier-1 is now LOCAL dev work, not operator-blocked; only tier 2/3 (real fleet) need provisioning.\nROOT-CAUSE CORRECTION (operator 2026-09-04): the WireGuard spike attributed the failed wg data-plane session to the Lima guest KERNEL (7.0.0-30-generic). That is almost certainly WRONG -- the operator runs FOUR physical nodes on that EXACT kernel connected via Tailscale (WireGuard), working fine. The kernel is NOT the root cause. The wg-session failure on the Lima guest (wg set OK but no UDP listen socket, zero underlay packets) has some OTHER cause -- the spike's netns/wg setup, a Lima networking/config quirk, or missing config -- to be diagnosed PROPERLY (evidence, not vibes) when building tier-1. Do NOT conclude 'Lima cannot do WireGuard' from the spike, and do NOT blame the kernel.\nFOOTPRINT CORRECTION (mayor, 2026-09-05): real Phase-3 per-node eBPF conntrack map footprint is ~1.82 MiB (1,909,072 bytes, measured on the #1568+#1567 merge-queue branch gh-readonly-queue/main/pr-1568-e221e354, run 33897608610) -- NOT the ~21.5 KiB previously quoted everywhere. That 21.5 KiB was the Phase-2 single-flow FIXTURE sizing; pa0ze Phase-3 (#1567) sized the LRU_HASH FWD_FLOW/REV_FLOW maps to their real 8192-entry capacity. LRU_HASH PREALLOCATES for max_entries, so ~1.82 MiB is a CONSTANT per-node cost independent of active-flow count. Use ~1.82 MiB (not 21.5 KiB) in the RSS/footprint go/no-go for the 1GB/1vCPU target node. Still small (~0.18% of 1 GiB), but the prior figure was wrong by ~85x.\nGATE 1 TIER-1 STATUS (2026-09-08, aie31.5/aie31.7, PR #1619): NOT YET PROVEN end-to-end -- but 2 of 3 local blockers are now resolved with evidence, and a NEW, more specific blocker was found and isolated.\n\n(1) aie31.5 L2-header-skip fix: LANDED. uplink_ingress/try_uplink_egress_return now resolve the uplink's L2 header length (14 for a real Ethernet NIC/veth, 0 for WireGuard/any L3-only device) via the loader querying the interface's Linux ARPHRD_* type from sysfs and writing it into CONFIG, instead of hard-coding ETH_HLEN=14. Fail-on-revert unit tests added; scripts/servicelb/smoke.sh (single-node L2) still passes unchanged.\n\n(2) WIREGUARD-SESSION MYSTERY: SOLVED, root cause was NOT the kernel (confirming the operator's 2026-09-04 correction). Two distinct, evidenced local causes: (a) Ubuntu ships an AppArmor profile (/etc/apparmor.d/wg) confining /usr/bin/wg to file rw ONLY under /etc/wireguard/** -- no dac_override/dac_read_search capability -- so `wg set --private-key \u003cpath\u003e` on a key anywhere else fails (`fopen: Permission denied`) even as root; confirmed via dmesg `apparmor=\"DENIED\" ... capname=\"dac_read_search\"/\"dac_override\"`. (b) the kernel does not bind the WireGuard UDP listen socket until the interface is administratively brought UP -- `wg set \u003cif\u003e listen-port N` on a still-down device reports success and `wg show` echoes the port back, but no socket appears in `ss -uln`/proc/net/udp until immediately after `ip link set \u003cif\u003e up`. Both fixed in scripts/servicelb/smoke-wg-2node-remote.sh's setup order (key under /etc/wireguard/, `up` after config).\n\n(3) With (1)+(2) fixed, a REAL 2-Lima-VM WireGuard tunnel (lima-node-2 \u003c-\u003e lima-node-4, same Lima network, real underlay eth0 addresses as WG endpoints) comes up correctly (ping across the tunnel + tcpdump-confirmed real WG handshake/data packets on the underlay). The servicelb-ebpf loader attaches cleanly to wg0 on both nodes (verifier-accept PASS, \"L2 header skip 0 byte(s)\" confirmed in the loader's own log) and aie31.5's fix is verified against REAL WireGuard traffic: a client SYN arriving on node2's wg0 correctly classifies into FWD_PENDING with the exact right 5-tuple (real client IP, real ports, VIP).\n\n(4) NEW BLOCKER FOUND (filed as mayor-f3ru5, blocks aie31.7's remaining scope item): the subsequent `bpf_redirect(geneve_ifindex, 0)` that hands the classified/tunnel-keyed packet to geneve0 for encapsulation is dropped INSIDE THE KERNEL when the packet arrived on an L3-only source device (wg0) but the redirect target (geneve0) is Ethernet-type -- confirmed via `trace-cmd record -e skb:kfree_skb`: freed at `location=__bpf_redirect+0x220 reason: NOT_SPECIFIED`, corroborated by geneve0's TX `dropped` counter incrementing exactly once per client attempt with zero RX on the peer. The identical redirect from a real (Ethernet-type) veth source to geneve0 works fine (smoke.sh passes) -- this is specific to the L3-source/L2-destination device crossing, not a generic redirect or Geneve bug.\n\nNET: Gate 1 Tier-1 mechanism is CLOSE but not proven -- the wg-uplink parse path and the WG transport itself are both now solid and evidenced; the remaining gap is a kernel-level bpf_redirect incompatibility, isolated to one specific call site pattern, with a reproducible rig (scripts/servicelb/smoke-wg-2node.sh) that gets to this exact failure automatically and prints the same evidence every time. See mayor-f3ru5 for likely fix directions (synthesizing an L2 header via ctx.adjust_room before redirect) -- not yet attempted.","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:15Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:15Z","dependencies":[{"issue_id":"mayor-lrbvo","depends_on_id":"mayor-aie31","type":"discovered-from","created_at":"2026-09-09T00:41:34Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"mayor-f3ru5","title":"servicelb-ebpf: bpf_redirect fails crossing L3-only wg0 uplink into Ethernet-type geneve0 device","description":"Discovered while proving aie31.7's 2-Lima-WireGuard cross-node round trip (after the wg-session issue and aie31.5's L2-header-skip fix were both resolved). uplink_ingress/try_uplink_egress_return correctly classify a real client SYN arriving on a WireGuard (L3-only, ARPHRD_NONE) uplink -- confirmed via a live FWD_PENDING dump showing a correctly-decoded 5-tuple -- but the subsequent bpf_redirect(geneve_ifindex, 0) that hands the packet to geneve0 (always Ethernet-type/ARPHRD_ETHER) for encapsulation is dropped INSIDE THE KERNEL, not by the eBPF verifier or program logic.\n\nEVIDENCE: `sudo trace-cmd record -e skb:kfree_skb` during a client attempt shows the encapsulated SYN freed at `location=__bpf_redirect+0x220 reason: NOT_SPECIFIED` on the napi/wg0 receive softirq. Corroborated by `ip -s link show geneve0`: TX `dropped` counter increments by exactly 1 per client attempt, with 0 corresponding RX on the peer node's geneve0/wg0 -- the packet never leaves the ingress node at all. The same veth-sourced (Ethernet-type) redirect to geneve0 works fine (scripts/servicelb/smoke.sh passes), so this is specific to the SOURCE device being L3-only (no L2 header, hard_header_len=0) while the REDIRECT TARGET (geneve0) is Ethernet-type and presumably expects a mac header the L3-sourced skb never carries.\n\nReproduction: `scripts/servicelb/smoke-wg-2node.sh --vm-a lima-node-2 --vm-b lima-node-4` (or any 2 Lima VMs on the same network) -- runs to this exact failure point automatically and prints the same evidence via its dump-evidence step.\n\nLikely fix directions (not yet attempted, needs its own investigation): synthesize a minimal/dummy L2 header on the skb (e.g. via ctx.adjust_room with BPF_ADJ_ROOM_MAC) before redirecting from an L3-only uplink into geneve0, on both the forward (uplink_ingress) and return (try_uplink_egress_return) redirect sites. Also note: if a real L2 header ever needs synthesizing on encap, the DECAP side's inner-packet parsing (try_geneve_decap_forward/_return, still hard-coded to ETH_HLEN=14 per aie31.5's design note) may need matching review, since a WireGuard-sourced forward leg would then have to carry a real 14-byte header end-to-end through the Geneve payload for that assumption to keep holding.\n\nBlocks the remaining scope item of aie31.7 (\\\"drive a client-\u003eVIP-\u003ecross-node-backend-\u003eclient symmetric-return round-trip, assert it completes\\\") and Gate 1's Tier-1 sign-off in mayor-lrbvo.\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:14Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:14Z","dependencies":[{"issue_id":"mayor-f3ru5","depends_on_id":"mayor-aie31","type":"discovered-from","created_at":"2026-09-09T00:41:34Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.7","title":"build the 2-Lima-over-WireGuard tier-1 cross-node eBPF test setup (Phase-4 gate-1 MVP, local, no external fleet)","description":"The operator-agreed tier-1 Phase-4 MVP (lrbvo): test the eBPF ServiceLB cross-node symmetric-return LOCALLY across 2 Lima VMs on DISJOINT virtual networks (user-v2-workers-a vs -b -- genuinely separate L2 segments/subnets, per lima-start.sh) connected via WireGuard. No external fleet needed. Non-trivial infra effort -- hence its own ticket.\n\nSCOPE: (1) bring up 2 Lima VMs on the 2 disjoint networks; (2) establish a real WireGuard link between them (the 2026-09-04 spike's 1-VM two-netns approximation is NOT enough -- this needs the real 2-VM cross-network path, which lima-start.sh currently hard-refuses to route between disjoint networks -- WireGuard is the intended L3 overlay that provides it); (3) load the servicelb eBPF on both, attach on the wg uplink, drive a client-\u003eVIP-\u003ecross-node-backend-\u003eclient symmetric-return round-trip, assert it completes.\n\nBLOCKERS (both from the 2026-09-04 WireGuard spike, see lrbvo notes): (a) mayor-aie31.5 -- the servicelb-ebpf L2-header (ETH_HLEN=14) assumption that silently no-ops on an L3/no-L2 wg uplink (MUST land first); (b) the wg-data-plane-session-on-Lima issue (wg set OK but no UDP listen socket appeared -- NOT a kernel problem per operator, who runs 4 physical nodes on that exact kernel over Tailscale; diagnose the Lima-guest setup properly, evidence not vibes).\n\nDEPENDS ON: mayor-aie31.5 (L2 fix) AND pa0ze/#1567 merging first (shared servicelb-ebpf surface -- avoid the collision). ALREADY PROVEN by the spike: tc-bpf ATTACH to an L3/no-L2 wg interface WORKS. Once green: satisfies lrbvo gate-1 tier-1 (mechanism); tier 2/3 (real-internet fidelity) remain operator-provisioned.\n","notes":"PARTIAL PROGRESS (2026-09-08, PR #1619): the 2-Lima-VM WireGuard rig is built (scripts/servicelb/smoke-wg-2node.sh + smoke-wg-2node-remote.sh) and both local blockers named in this bead's own description are SOLVED with evidence: (a) aie31.5 landed (depends-on satisfied); (b) the wg-data-plane-session issue was root-caused as two Ubuntu/kernel-behavior specifics, not the guest kernel -- see mayor-lrbvo's notes for the full writeup (AppArmor confining /usr/bin/wg to /etc/wireguard/**, and the WG UDP socket only binding once the interface is administratively up).\n\nWith both fixed, the tunnel comes up for real and aie31.5's fix is verified against real WireGuard traffic. This bead's SCOPE ITEM (3) -- \"drive a client-\u003eVIP-\u003ecross-node-backend-\u003eclient symmetric-return round-trip, assert it completes\" -- is NOT yet satisfied: a newly-discovered kernel-level bpf_redirect(L3-only-source -\u003e Ethernet-type-destination) issue blocks the packet from ever reaching geneve0's encapsulation path. Filed as mayor-f3ru5 (now a blocked-by dependency of this bead) with full evidence and likely fix directions. Leaving this bead OPEN, blocked on mayor-f3ru5, rather than closing on partial completion.","status":"in_progress","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:09Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:50Z","started_at":"2026-09-08T15:40:50Z","dependencies":[{"issue_id":"mayor-aie31.7","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:02Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.7","depends_on_id":"mayor-f3ru5","type":"blocks","created_at":"2026-09-09T00:41:32Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.6","title":"eBPF LB memory-measurement tooling (lrbvo gate-2): bpftool map-memory sampler + servicelb-loader RSS","description":"Phase-4 gate-2 (lrbvo) requires eBPF LB memory 'independently and continuously monitorable' for BOTH kernel maps and userspace -- but neither is wired into any harness today (researcher 2026-09-04). The methods are proven + documented (crates/servicelb/README.md L68-92; docs/decisions/ebpf-toolchain-aya.md L33-41) but MANUAL-only. TWO small additions: (1) KERNEL: a sampler running 'bpftool map show' (actual live per-map memory, not just the max_entries ceiling) at intervals -\u003e CSV/export, for the servicelb maps. (2) USERSPACE: add the u7s-servicelb loader PID to scripts/conformance/sample-run-metrics.sh's sampling loop -- it currently samples apiserver/scheduler/konnectivity/node procs but NOT the loader (L235-263). Both small; needed before lrbvo gate-2 can produce a trustworthy MEASURED footprint (loxilb precedent: pre-allocated maps hide a floor until run). NOTE: post-pa0ze the conntrack maps are LRU_HASH (shared, single table), so the real footprint is far below the doc's PERCPU per-CPU 4-7 MiB estimate -- measure, do not estimate. Downstream of pa0ze/#1567; dispatch when Phase-4 work begins.\n","notes":"OPERATOR (2026-09-04): eBPF memory monitoring is a MUST -- 'without it we are blind to any of our in-kernel memory use.' Bumped P3-\u003eP2, moved from deferred to ACTIVE. Research/gap-analysis DONE (methods proven, nothing wired into any harness -- see this bead's desc). Next: DESIGN the monitoring setup (dispatched now), then a follow-on implements. Goal is CONTINUOUS monitoring, not a one-shot snapshot.","status":"closed","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:08Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:55Z","closed_at":"2026-09-08T15:40:55Z","close_reason":"Design complete: ai/findings/2026-09-04-mayor-aie31.6-ebpf-memory-monitoring-design.md (bpftool bytes_memlock field verified live on lima-node-smoke; CI feasibility confirmed via zz-ebpf-ci-spike run 33882862825). Follow-on implementation bead: mayor-tk4ku","dependencies":[{"issue_id":"mayor-aie31.6","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:01Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.24","title":"servicelb: reject --vip-ip that falls inside the pod CIDR","description":"front-IP space and pod CIDR are only disjoint by today's configuration, not by construction (mayor-aie31.12): a hostNetwork Pod's IP equals its node's IP, i.e. front-IP space, so a forward and reverse flow key can byte-collide. The --vip-ip flag (src/main.rs:56) is unvalidated. The loader should reject a VIP that falls inside the pod CIDR. Doc-side correction landed in mayor-aie31.12; this bead tracks the code-side validation only.\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:04Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:04Z","dependencies":[{"issue_id":"mayor-aie31.24","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:12Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.24","depends_on_id":"mayor-aie31.12","type":"discovered-from","created_at":"2026-09-09T00:41:33Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.21","title":"design(servicelb): per-flow backend affinity pinning + endpoint-removal conntrack eviction (Phase 5)","description":"Phase 5 needs one coherent decision on flow-to-backend affinity: the forward\npath must pin a backend per flow rather than re-resolving VIP_MAP per packet,\nAND endpoint removal must selectively evict the conntrack entries that point\nat the departed pod. These pull in opposite directions and cannot be designed\nseparately.\n\nTwo problems, one decision:\n\n1. No per-flow backend affinity. uplink_ingress chooses the backend from\n   VIP_MAP on every forward packet at servicelb-ebpf/src/main.rs:227, and\n   FWD_FLOW only records which node the packet went to for the return-path\n   existence check at line 496, where the value is read and discarded (see\n   aie31.20). With today's one-static-backend fixture this is invisible. With\n   real multi-endpoint Services in Phase 5, repointing a VIP mid-connection\n   immediately moves live traffic to a backend that holds no TCP state for\n   it, breaking the connection. Real load balancing needs the backend chosen\n   once per flow and stored in conntrack, with later packets following the\n   stored choice. That is what conntrack affinity is for, and the current\n   shape does not do it.\n\n2. No selective eviction on endpoint removal. When a pod leaves the\n   EndpointSlice, established flows pinned to it are orphaned. kube-proxy\n   actively flushes conntrack for a removed endpoint, which matters most for\n   UDP, which has no teardown to age the entry out. The eBPF equivalent is a\n   targeted pass that deletes FWD_FLOW and REV_FLOW entries pointing at the\n   removed pod. This is a deliberate scoped delete, not a reset, and it is\n   missing.\n\nThe tension: affinity pinning says do NOT disturb a live flow on an unrelated\nconfig change, while eviction says DO disturb the specific flows whose\nbackend is gone. The decision has to state exactly which config changes\ndisturb which flows, mirroring kube-proxy's terminating-endpoint and\ngraceful-termination semantics.\n\nInvariant this rests on, now recorded in aie31.8: reconciliation is a live\nper-key map write to VIP_MAP and POD_TARGETS, NOT a program reload or a\nprocess restart, so it never by itself resets the LRU conntrack maps. The\norphaning above is semantic, not a map reset.\n\nOverlaps aie31.10, since keying the eviction pass needs a richer POD_TARGETS\nthan one port per pod. Phase 5 scope, mayor-9gr0n.\n\nDONE WHEN: a written decision defines the per-flow affinity model and the\nexact set of config changes that trigger selective conntrack eviction, with\nUDP called out explicitly.\n\n","notes":"Findings: ai/findings/2026-09-06-mayor-aie31.21-affinity-pinning-eviction.md (pushed on worker/agent-a86241176a94239ea, no PR). RECOMMENDATION: stored per-flow affinity -- FWD_FLOW pins the full backend identity (backend_node_ip + pod_ip), chosen once on the first-packet miss via a deterministic hash over the ready set; later packets follow the pin instead of re-resolving VIP_MAP. Pair with scoped event-driven eviction on endpoint removal: UDP delete of FWD_FLOW pins to the departed pod is MANDATORY (no teardown; one-way UDP refreshes LRU forever), TCP self-heals via reset (delete optional/cleanliness), REV_FLOW deleted for BOTH protocols on the pod's host node to close the pod-IP-reuse hazard. Reconcile rule: only removal of the specific endpoint a flow is pinned to disturbs that flow; add/terminating-but-serving/policy/topology never evict -- mirrors kube-proxy's UDP-only CleanStaleEntries. COUPLING: conflicts with aie31.20 (affinity keeps FWD_FLOW alive -- re-scope .20 to the return-auth MAC only); composes with aie31.11 (owns WHEN a flow is admitted; this bead owns WHAT it stores + WHEN evicted); aie31.10 already closed (TARGET_PORTS keys on front tuple, so eviction indexes off FWD_FLOW.value.pod_ip, not TARGET_PORTS). Awaiting operator decision.\n## critical-reviewer findings — findings-doc — mayor-aie31.21\n\n**Verdict**: needs-discussion\n\n**Confirmed findings** (must be true, evidence cited):\n- [HIGH] \"TCP self-heals via reset\" and open-question-1's \"correctness is identical either way\" do not hold under the doc's own Option A model. A strictly-pinned flow never rehashes to a different backend, so no live host ever receives the packet to emit an RST; the doc's own UDP-mandatory rationale (\"refreshes FWD_FLOW LRU recency on every packet, so LRU pressure alone never reclaims it\") applies equally to a TCP flow retrying against the departed pod, since later packets hit-not-reinsert FWD_FLOW (doc's own Option A: \"FWD_FLOW hit -\u003e follow the stored pin, skip VIP_MAP resolution\"). Evidence: doc lines 123-135 (UDP/TCP eviction rationale) vs. `crates/servicelb/servicelb-ebpf/src/main.rs:235-246,514` (forward hit skips re-resolution; return decap is existence-only). Suggested fix: reframe TCP eviction as a bounded-hang-vs-instant-RST tradeoff (real availability cost), not a correctness-neutral cleanliness choice, before the operator answers open question 1.\n- [LOW] The Recommendation's \"This mirrors kube-proxy's conntrack reconciler exactly\" (doc:29) is contradicted a few sections later by the doc's own REV_FLOW-deleted-for-both-protocols mechanism (doc:136-143), which the doc itself says \"kube-proxy's kernel conntrack has no analogue for.\" Suggested fix: drop \"exactly\" or scope the mirror claim to the UDP/FWD_FLOW row only.\n\n**Suspicions** (worth the maintainer's attention, but I did not confirm):\n- aie31.11's own findings (bd notes on mayor-aie31.11, same day) recommend splitting FWD_FLOW into FWD_PENDING (2048) + FWD_MAIN (8192) tiers; this doc's eviction mechanism sketch scans one 8192-entry FWD_FLOW table. Why suspicious: doc marks aie31.11 as \"COMPOSES\" without addressing whether eviction/promotion needs to cover both tiers. To verify: revisit the eviction mechanism sketch once aie31.11's split is decided.\n\n**Not reviewed**: Phase-5 VIP_MAP multi-endpoint representation (explicitly out of scope per the doc, deferred to mayor-9gr0n); live-kernel confirmation of LRU-recency-refresh-on-lookup (well-established BPF_MAP_TYPE_LRU_HASH kernel behavior, not re-verified via VM per test-execution posture — the finding above is derived from the doc's own internal argument, not from a fresh kernel test).\n\n**Meta**: reviewed at 2026-09-06T09:47:20Z, deliverable size (1 file / 243 LoC, commit bea8be7d), checklist items checked (5/5 findings-doc checklist items: citation quality, verdict soundness, follow-on beads, uncertainty language, file location).\nDECISION (operator 2026-09-07): ACCEPT Option A -- stored per-flow affinity with the FULL backend identity (backend_node_ip AND pod_ip) written into FWD_FLOW on the first-packet miss; later packets follow the stored pin. Pair with scoped event-driven eviction (UDP FWD_FLOW deletion mandatory; REV_FLOW deletion both protocols for pod-IP-reuse safety). CONSEQUENCE: aie31.20 (eliminate FWD_FLOW via stateless-return-MAC) is DEAD as map-elimination -- FWD_FLOW is now the affinity store; re-scope aie31.20 to return-path auth MAC only, or supersede. Cross-node drift (1dwwz): the delivery-node-authoritative-validation invariant (tavxy) bounds ingress drift to a safe drop. OPEN SUB-QUESTION: TCP eviction yes/no (dksf5) is NOT settled by 'stored full backend identity' -- surfaced to operator. Implementation pending under aie31 phases; stays open as impl-ready.\nTCP-eviction decision (operator 2026-09-07, via dksf5): YES. Full Option-A eviction table is now settled: UDP FWD_FLOW deletion mandatory; TCP FWD_FLOW deletion YES (prompt RST beats a bounded multi-minute hang); REV_FLOW deletion both protocols (pod-IP-reuse safety). dksf5 closed.","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:01Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:01Z","dependencies":[{"issue_id":"mayor-aie31.21","depends_on_id":"mayor-9gr0n","type":"relates-to","created_at":"2026-09-09T00:41:36Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.21","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:10Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.17","title":"design(servicelb): unified flow table with an explicit role tag","description":"Merge FWD_FLOW and REV_FLOW into one table discriminated by an explicit tag\nrather than by address disjointness, and settle the ceiling as part of it.\n\nMeasured on a live kernel via bpftool, so these are exact rather than\nmodelled. All LRU_HASH, bytes_memlock:\n\n  forward map today, key 37 value 4, 8192 entries        918,400\n  reverse map today, key 37 value 12, 8192 entries       983,936\n  pair today                                           1,902,336\n  unified, key 38 value 12, 4096 entries                 492,416\n  unified, key 38 value 12, 8192 entries                 983,936\n  unified, key 38 value 12, 16384 entries              1,966,976\n\nThe CI sampler total across all five servicelb maps is 1,909,072, leaving\n6,736 for CONFIG, VIP_MAP, and POD_TARGETS.\n\nThe tag byte is free. BPF rounds key_size up to a multiple of 8 when sizing\nelements, confirmed by measurement rather than by reading hashtab.c: key\nsizes 36 through 40 all cost the same, and 41 jumps by exactly 8 bytes per\nentry. See bd memory ebpf-map-key-size-rounds-up-to-8 for the numbers and the\nreproduction method. That is why the unified 8192 row above is byte-identical\nto today's reverse map on its own.\n\nAlternative worth weighing and recording: the tag does not strictly need a\n38th byte. The existing proto byte holds 6 or 17 and has six spare bits, so a\nhigh bit could carry direction and a second bit QUIC, keeping the key at 37\nand touching no call sites. The explicit byte is recommended anyway, since it\ncosts nothing and is self-documenting, but the choice should be written down.\n\nDo NOT rest this on VIP-space versus pod-CIDR disjointness. That invariant\ndoes not hold for hostNetwork Pods; see the disjointness bead. Note also that\nthe two maps carry different value types today, 4 bytes against 12, so the\ntag has to select the value shape as well as the key population.\n\nCapacity tradeoff to decide, not assume. Unifying at 8192 halves memory and\nkeeps full capacity for a single-role node, but halves worst-case capacity\nfor a node serving both roles. Unifying at 16384 costs 64,640 bytes more than\ntoday and yields 16384 flexible slots instead of a fixed 8192 plus 8192,\nwhich strictly dominates. Bucket arrays round to a power of two, so those are\nthe only sensible stops.\n\nQUIC folds in as a third population under the same tag once native QUIC\nlands, rather than needing a fourth map.\n\nAlso update ai/extended-context/ebpf-lb-dataplane.md line 96, which states\nthe key as 37 bytes.\n\nDONE WHEN: one tagged table exists, its value shape covers both roles, and\nthe ceiling choice is recorded with its reasoning.\n\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:57Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:57Z","dependencies":[{"issue_id":"mayor-aie31.17","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:08Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.17","depends_on_id":"mayor-aie31.11","type":"blocks","created_at":"2026-09-09T00:41:27Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.17","depends_on_id":"mayor-aie31.12","type":"blocks","created_at":"2026-09-09T00:41:27Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.16","title":"perf(servicelb): release the aya object after attach so the DaemonSet idles thin","description":"Pin the maps, drop the aya Ebpf object after attach, trim the heap, and\nreopen maps from their pins when they are next needed, so the DaemonSet\ncontainer idles thin instead of holding load-time state forever.\n\nThe blocking loop at src/main.rs:204 is correct and must stay. A DaemonSet\nPod only accepts restartPolicy Always, so exiting would be restarted and\neventually backed off. The cost is not the sleep, it is what the process\nholds while sleeping. Ebpf::load parses the object at line 153 and keeps the\nBTF, relocation state, and program registry alive for the whole process\nlifetime, and the 214 KiB embedded blob is resident once touched. Glibc does\nnot return that heap on its own, so resident memory sits at the load-time\nhigh-water mark, which in a container is what the memory request has to cover\non every node.\n\nMapData::from_pin exists in aya 0.14 for reopening. Phase 5's watcher needs\nmap handles, not the parsed ELF.\n\nWorth recording while here: since Linux 5.11, BPF map memory is charged to\nthe creating process's cgroup, so the roughly 1.82 MiB of map memory counts\nagainst this container's own memory limit rather than sitting outside it. Map\nsizing and container limits are one budget, not two. The bump_memlock_rlimit\ncall at src/main.rs:296 only matters for kernels older than that.\n\nDONE WHEN: steady-state RSS in the existing loader-rss.csv sampler drops\nmeasurably below the load-time peak, with the attachment still live.\n\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:56Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:56Z","dependencies":[{"issue_id":"mayor-aie31.16","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:07Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.16","depends_on_id":"mayor-aie31.8","type":"blocks","created_at":"2026-09-09T00:41:26Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.15","title":"perf(servicelb): make the per-packet flow-table writes conditional","description":"Replace the unconditional per-packet map inserts with\nlookup-then-write-on-miss.\n\nservicelb-ebpf/src/main.rs:238 inserts into FWD_FLOW on every forwarded\npacket, and line 418 inserts into REV_FLOW on every decapped packet, not just\non the first packet of a flow. An LRU hash update takes the bucket\nraw_spinlock and can run the LRU shrink path. A lookup is an RCU read that\nalready refreshes the entry's LRU reference bit, so recency is preserved\nwithout the write. The difference is largest under multi-core load, where\nseveral CPUs serialize on one bucket lock for a single busy flow.\n\nFWD_FLOW's value is constant per flow, so skipping the rewrite is trivially\nsafe. REV_FLOW's value can legitimately change if the ingress node changes,\nso the check there is exists-and-matches rather than plain existence.\n\nDONE WHEN: steady-state packets of an established flow take the read path,\nverified by a map-update counter that stays flat while traffic flows.\n\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:55Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:55Z","dependencies":[{"issue_id":"mayor-aie31.15","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:07Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.14","title":"perf(servicelb): gate the egress conntrack lookup on POD_TARGETS membership","description":"Gate the uplink-egress conntrack lookup on POD_TARGETS membership, checking\nonly that the source address is a backend Pod on this node and deliberately\nnot comparing the port.\n\ntry_uplink_egress_return at servicelb-ebpf/src/main.rs:537 performs a\n40-byte-key LRU hash lookup on every TCP and UDP packet leaving the node\nbefore it can reject non-ServiceLB traffic. POD_TARGETS is a 32-entry map\nwith a 4-byte key, so probing it first is a far cheaper hash and it\nshort-circuits before the 37-byte key is even built on the stack.\n\nMembership only, not a port match. Comparing the packet's source port against\nthe current POD_TARGETS value would couple the return path to a value that\ncan change under it during a rolling update or a targetPort change, dropping\nlive flows whose reverse entry was written under the old port. The current\ncode cannot produce that failure, because it matches purely on the stored\ntuple. Membership is safe by construction: a flow only has a reverse entry if\nthe forward path found its pod in this map, so anything still live passes,\nand a pod absent from the map is going away regardless.\n\nPairs with the REV_FLOW-miss bead, which needs exactly this positive\nidentification before it can safely turn a miss into a drop.\n\nDONE WHEN: traffic not sourced from a backend Pod rejects without touching\nthe conntrack table, and a test covers that a targetPort change does not\nbreak an in-flight flow.\n\n","status":"closed","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:54Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:54Z","closed_at":"2026-09-08T15:40:54Z","close_reason":"PR #1624 opened","dependencies":[{"issue_id":"mayor-aie31.14","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:06Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.14","depends_on_id":"mayor-tavxy","type":"relates-to","created_at":"2026-09-09T00:41:38Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.13","title":"perf(servicelb): direct packet access instead of bpf_skb_load_bytes on both uplink hooks","description":"Replace ctx.load with direct packet access on both uplink hooks, because\nevery ctx.load is a bpf_skb_load_bytes helper call and these hooks see all\nnode traffic, not just ServiceLB's.\n\nConfirmed in aya-ebpf 0.2.1: TcContext::load delegates to SkBuff::load, which\ncalls bpf_skb_load_bytes. Cost per packet before either hook can even reject\nnon-ServiceLB traffic:\n\n- try_uplink_egress_return, servicelb-ebpf/src/main.rs:537. Seven helper\n  calls plus a 40-byte-key LRU hash lookup.\n- try_uplink_ingress, servicelb-ebpf/src/main.rs:207. Five helper calls plus\n  a VIP_MAP lookup.\n\nThat is paid by kubelet, etcd, image pulls, flannel, and everything else\nentering or leaving the node. ctx.data and ctx.data_end are available in this\naya version, turning each read into a plain load behind one bounds check.\nAdjacent fields can also coalesce: the source and destination addresses are\ncontiguous, as are the two ports.\n\nCaution: direct packet access covers the linear head only. Headers are linear\nin practice for tc ingress and for egress from a local socket, but any read\nthat reaches into a payload, which native QUIC will need for the DCID,\nrequires bpf_skb_pull_data or a load-bytes fallback.\n\nThis gets more valuable under the dual-stack and QUIC requirements, not less.\nIPv6 means branching on ethertype with different header offsets and walking\nextension headers, each of which is another helper call under the current\napproach.\n\nDONE WHEN: header reads go through ctx.data and ctx.data_end with\nverifier-accepted bounds checks, and the servicelb smoke fixture still passes\nend to end.\n\n","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:53Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:53Z","dependencies":[{"issue_id":"mayor-aie31.13","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:06Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.12","title":"docs(ebpf-lb): correct the VIP-space vs pod-CIDR disjointness claim","description":"The claim that VIP-space and the pod CIDR are disjoint by construction is\nfalse for hostNetwork Pods, and the doc uses it to justify a design choice.\n\nai/extended-context/ebpf-lb-dataplane.md line 89 states the two ranges never\ncollide and concludes that one map per protocol can cover both the forward\nand reverse roles. A Pod with hostNetwork true has a pod IP equal to its node\nIP, which is exactly VIP space under the node-owned-address model. That is\nstandard Kubernetes semantics, not misconfiguration, and this repo already\nships two hostNetwork DaemonSets in manifests/kube-proxy.yaml and\nmanifests/flannel.yaml. The doc specifies the servicelb control plane itself\nas hostNetwork at line 131.\n\nConcrete collision, node N at 192.168.109.3. A forward key for a service\nfronted at N holds N's address in its other-address slot. A reverse key for a\nhostNetwork backend on N holds the same address in the same slot. A client\nlegally reusing one local port across both makes the two 37-byte keys\nbyte-identical.\n\nThe ranges are also only disjoint by configuration today, not by\nconstruction. The pod CIDR is 10.244.0.0/16 from scripts/install.sh:890 and\nnode addresses sit on 192.168.x.x, set by unrelated mechanisms with no\ncross-validation. The --vip-ip flag at src/main.rs:56 is an unvalidated\naddress with no check that it is the node's own or outside the pod CIDR.\n\nCalibration note: the same paragraph of this doc was already wrong once,\nspecifying LRU_PERCPU_HASH, which was empirically disproved and corrected.\nThat is reason to audit the neighbouring sentence rather than defer to it.\n\nConsider merging with aie31.3, which corrects the map type and footprint in\nthe same file. Kept separate here because this one also needs a code change.\n\nDONE WHEN: the doc states the real constraint rather than a false invariant,\nand the loader rejects a VIP that falls inside the pod CIDR.\n\n","status":"closed","priority":2,"issue_type":"bug","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:52Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:54Z","closed_at":"2026-09-08T15:40:54Z","close_reason":"Doc correction landed in PR #1614; loader-side validation (also required by this bead's done-when) is out of scope for this doc-only PR and tracked separately as mayor-aie31.24","dependencies":[{"issue_id":"mayor-aie31.12","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:05Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.1","title":"net/servicelb: Tier-1 eBPF smoke harness -- load + verifier-accept + single round-trip (local-gated)","description":"Codify the currently-hand-done eBPF dataplane verification into a reproducible script, so the servicelb eBPF programs have an in-kernel gate. Today there is NONE: Phase 2 (g7jh2/#1557) was hand-verified on an ad-hoc Lima VM, and CI ebpf-build only runs clippy/fmt/build (the bpfel build proves it COMPILES, not that the kernel verifier ACCEPTS it at load).\n\nScope (Tier 1 only): a script (reusing scripts/conformance/lima-start.sh patterns) that (1) brings up a Lima VM with the geneve device + CAP_BPF, (2) builds and LOADS the servicelb eBPF programs, (3) asserts the kernel verifier accepts them (this catches the exact verifier-rejection-under-churn risk flagged on pa0ze), and (4) drives one client-\u003eVIP-\u003ebackend round-trip and asserts it completes. Local-gated for eBPF PRs first; evaluate CI promotion separately (needs a spike on whether the e2e-focus runners allow privileged tc-bpf load).\n\nOut of scope (Tier 2 -\u003e Phase 4/lrbvo): concurrent/churning-flow load, LRU-eviction survival, RSS measurement. lrbvo already owns real-fleet return-path + RSS.\n\nThis becomes pa0ze verifier gate (replaces its unit-tests-only scope). Operator decision 2026-09-04: Tier-1-now, recommended. Cross-ref: pa0ze, lrbvo, de18e (ebpf-build must also EXECUTE servicelb cargo tests), and ai/extended-context/ebpf-lb-dataplane.md.\n","status":"closed","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:49Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:55Z","closed_at":"2026-09-08T15:40:55Z","close_reason":"PR #1565 opened","dependencies":[{"issue_id":"mayor-aie31.1","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:42:16Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-9gr0n","title":"net/servicelb: userspace controller -- watch type=LoadBalancer Services + EndpointSlice, program dataplane maps","description":"Phase 5 of mayor-aie31 (ServiceLB implementation epic). Per-node\nuserspace control plane per ai/extended-context/ebpf-lb-dataplane.md's\n\"Userspace control plane\" section: one DaemonSet per node, hostNetwork\nplus CAP_BPF/CAP_NET_ADMIN, no CRI socket. Loads and pins the Phase 1\nthrough 3 tc-bpf programs once, then watches Service (type=LoadBalancer)\nand EndpointSlice via u7s's existing watch machinery (reuse, do not\nreimplement), and on change writes the VIP map, the endpoint map, and the\nbackend-local vni_to_pod map -- then idles. No persistent proxy loop; the\nkernel does the packet forwarding (mirrors klipper-lb's memory-discipline\nmodel, per cni-svclb-landscape.md).\n\nExtract the Service/EndpointSlice-diff-to-map-entries logic into a pure\nRust function, separate from the async watch-handler plumbing, so it is\ndirectly unit-testable (Rule 14) without a live cluster.\n\nDepends on Phase 4 (mayor-lrbvo) passing -- do not invest in the full\ncontroller ahead of the go/no-go gate; if gate 1 or 2 fails, the map\nschema or hook design this controller programs may need to change first.\nBlocks Phase 6 (kube-proxy/flannel coexistence verification) -- proving\ncoexistence needs a controller-driven system running real Service\nobjects, not Phase 2's static test fixture.\n\nRisk: MEDIUM -- lower than the dataplane phases since this reuses\nexisting watch code per the mechanism doc, but wrong map population\n(stale entries, missed EndpointSlice updates, VNI allocation bugs) is a\nsilent-wrong-routing failure mode, not a crash.\n\nVerification: unit tests for the pure reconcile function (Service/\nEndpointSlice fixtures in, expected map-entry diffs out, including add/\nupdate/delete and multi-EndpointSlice-per-Service cases). Integration\ntest on a Lima cluster: create/update/delete a real type=LoadBalancer\nService and confirm map contents via bpftool map dump match.\n","notes":"OPEN QUESTION (operator 2026-09-04): the Phase-5 DaemonSet needs a BUILT CONTAINER IMAGE pushed to a registry -- decide WHERE. Natural default to evaluate first: GHCR (ghcr.io/litehop/...) -- free for the org, integrated with the repo's GitHub Actions (build+push in the same CI that the eBPF-CI spike proved viable), and the DaemonSet pulls it directly with no extra infra; alternatives = Docker Hub or a self-hosted registry if there's a reason. Needed for Phase 5 regardless of the eventual servicelb repo-split (filed as a deferred aie31 child). Upstream-blocked by Phase 4 (lrbvo) anyway, so not urgent -- decide when Phase 5 starts.\nCORRECTION (operator 2026-09-04): GHCR is OUT -- it is IPv4-only, incompatible with our IPv6 targets. The binding constraint is a registry PULLABLE over IPv6 by (eventual) IPv6-only nodes (Scaleway etc.). Docker Hub is a candidate (operator has an account, push is possible from a dual-stack CI runner) but needs setup AND its registry endpoint (registry-1.docker.io) has historically been IPv4-only for PULLS too -- so VERIFY Docker Hub actually serves pulls over IPv6 before committing (do NOT repeat the GHCR assumption; a quick 'dig AAAA registry-1.docker.io' + a real v6 pull test settles it). If no hosted candidate serves IPv6 pulls, the real options are: (a) SELF-HOST an IPv6-capable registry (zot / Harbor / registry:2) on a dual-stack host -- full control; or (b) rely on NAT64/DNS64 at the IPv6-only node to reach an IPv4-only registry via translation (many IPv6-only clouds provide this) -- which would make GHCR/Docker Hub viable again through the gateway. Not urgent: Phase 5, downstream of Phase 4 (lrbvo); IPv6-only was descoped from the near-term MVP -- decide when Phase 5 starts, after verifying the chosen registry's actual IPv6-pull path.","status":"open","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:48Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:48Z","dependencies":[{"issue_id":"mayor-9gr0n","depends_on_id":"mayor-aie31","type":"discovered-from","created_at":"2026-09-09T00:41:33Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-9gr0n","depends_on_id":"mayor-aie31.21","type":"relates-to","created_at":"2026-09-09T00:41:36Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-9gr0n","depends_on_id":"mayor-lrbvo","type":"blocks","created_at":"2026-09-09T00:41:25Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"mayor-1dwwz","title":"design(servicelb): aie31.21 eviction ignores cross-node convergence drift - delivery node must be the authoritative consistency point","description":"Operator raised 2026-09-07: the aie31.21 affinity+eviction doc reasons about eviction as a per-node reconcile but never addresses the cross-node convergence WINDOW. Each node's DaemonSet processes the same EndpointSlice event at different times (uplink latency), so node eBPF state can drift apart for multiple seconds before converging. Governing principle to add to the doc: dataplane is eventually-consistent per node; correctness must NOT depend on synchronized updates; worst case during the window must be bounded transient drop/hang, NEVER misdelivery to a wrong live pod. Scenarios: new-flow selection divergence = safe (per-flow ECMP-sticky to one ingress node); stale FWD_FLOW pin during removal lag = blackhole/hang if IP not reused (bounded, ok) BUT misdelivery if the departed pod's IP is reused before the lagging ingress node evicts (UNSAFE - the drift window widens this). RESOLVING INVARIANT: make the delivery (backend-host) node validate each inbound Geneve-decap packet against its OWN current serving-set for the front IP before delivering - the host has a lag-free view of its local pods, so ingress drift degrades to a drop at the authoritative node, not a cross-service leak. OPEN QUESTION to verify against code: does the inbound decap-forward delivery path in crates/servicelb/servicelb-ebpf/src/main.rs currently do this membership check? aie31.14 gates EGRESS on POD_TARGETS (different hook) - may not cover inbound delivery. Action: amend aie31.21 doc with the cross-node-drift section + invariant; file a build bead if the delivery-side validation is missing. Part of parked aie31 epic.\n","notes":"Verdict: invariant does NOT hold today. try_geneve_decap_forward (servicelb-ebpf/src/main.rs:331-471) trusts the ingress-stamped Geneve pod_ip unconditionally and DNATs to it; its only check, TARGET_PORTS.get(front tuple) (main.rs:361-368), validates \"this node serves some backend for this front\", not \"this pod_ip is a current member\" -- no analogue of aie31.14's egress POD_TARGETS-membership gate exists on the inbound decap-delivery path. Drift verdict: new-flow divergence is SAFE (per-flow ECMP-sticky); stale FWD_FLOW pin without IP reuse is SAFE (bounded blackhole/hang); stale pin WITH pod-IP reuse before the lagging ingress evicts is UNSAFE -- misdelivers to the new pod, and the drift window directly widens this exposure. Doc amended: ai/findings/2026-09-06-mayor-aie31.21-affinity-pinning-eviction.md, new \"Cross-node convergence \u0026 drift\" section (keeps its aie31.21 header, doc-only PR under mayor-1dwwz). Build follow-on filed: mayor-tavxy (P2, parent aie31, cross-refs aie31.21 + aie31.14). Left OPEN per dispatch instructions -- operator resolves aie31.21 separately.","status":"in_progress","priority":2,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:46Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:49Z","started_at":"2026-09-08T15:40:49Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31","title":"epic: implement eBPF ServiceLB dataplane (symmetric-return Geneve, aya)","description":"Implement the ServiceLB dataplane settled by three ADRs (PR #1502):\ndocs/decisions/servicelb-ebpf-geneve-dataplane.md (per-node eBPF tc-bpf\npipeline, Geneve-encap cross-node delivery, real client IP at L3),\nservicelb-symmetric-geneve-return.md (reply routes back through the\ningress node, never DSR/source-forged), ebpf-toolchain-aya.md (aya, not\nlibbpf-rs/C). Mechanism detail: ai/extended-context/ebpf-lb-dataplane.md.\n\nScope: this dataplane IS u7s's type=LoadBalancer ServiceLB (supersedes the\nklipper-lb-alike, mayor-0gpqp, already closed). kube-proxy stays untouched\nfor east-west ClusterIP/NodePort traffic; this epic owns north-south\nLoadBalancer VIP:PORT traffic only.\n\nPhased implementation plan: ai/findings/2026-09-02-\u003cthis-epic-id\u003e-servicelb-impl-plan.md\n(written once this epic's ID is known). Child beads filed one per phase,\ndiscovered-from this epic, chained with bd dep for ordering. Design-phase\npredecessors (already closed, produced the ADRs): mayor-2et9d, mayor-fhfro\n(superseded), mayor-mma08.\n","notes":"filed mayor-g6u8s: Phase 1 -- aya eBPF program skeleton + build/load wiring\nfiled mayor-g7jh2: Phase 2 -- Geneve encap/decap on the symmetric-return path (single-flow happy path); depends on Phase 1 (mayor-g6u8s)\nfiled mayor-pa0ze: Phase 3 -- conntrack full-tuple keying + VIP/pod-identifier echo format; depends on Phase 2 (mayor-g7jh2)\nfiled mayor-lrbvo: Phase 4 -- prototype go/no-go gates (real-fleet return path + RSS); depends on Phase 3 (mayor-pa0ze)\nfiled mayor-9gr0n: Phase 5 -- userspace controller (watch type=LoadBalancer Services + EndpointSlice, program maps); depends on Phase 4 (mayor-lrbvo)\nfiled mayor-waqhd: Phase 6 -- coexistence verification with kube-proxy and flannel (east-west untouched); depends on Phase 5 (mayor-9gr0n)\nfiled mayor-g9l0f: Phase 7 (final) -- e2e/conformance gating (pick + wire sonobuoy specs); depends on Phase 6 (mayor-waqhd). All 7 phase beads now filed and chained.\nPhase 4 (mayor-lrbvo) clarification spike: operator's '2 disjoint Lima networks' counter-hypothesis is PARTIAL, not YES/NO -- see mayor-lrbvo note for full breakdown. Summary: disjoint L3 subnets are already true of user-v2-workers-a/-b (plan overstates this specific point), but there is zero built-in path between separate Lima networks today (hard blocker in current tooling, not fundamental) and even a hand-built relay wouldn't replicate the real fleet's provider-specific NAT/anti-spoof/Tailscale behavior (fidelity limitation) -- real-fleet access for gate 1 remains the open operational dependency. Gate 2 (RSS memory) needs no real fleet at all; already Lima-provable per the loxilb precedent.\nDECISION (operator, 2026-09-01): GO -- authorized to dispatch with no further sign-off. Gated only on (1) free VM capacity AND (2) no higher-priority correctness/security/perf tickets remaining. Feature-tier, so tail-of-drain. See bd memory servicelb-aie31-operator-go-tail-of-drain.\nCROSS-LINK from mayor-elmno (2026-09-02): when implementing the eBPF ServiceLB dataplane, evaluate enabling Flannel UNIFORMLY across environments (production already uses Flannel; the conformance harness (lima-start.sh) uses CRI-O's static bridge with hardcoded 10.85.\u003coctet\u003e.0/24 subnets + ipam disabled). Adopting Flannel in the harness too would make it CONSUME node.spec.podCIDR like prod, which activates #1525/#1527's podIP-allowlist in conformance and closes mayor-elmno's test-fidelity gap for free. See mayor-elmno notes for the full evidence (two-allocator drift breaks the allowlist on any non-default VM) + the consume-model refactor recipe if Flannel-uniform isn't chosen.\nCLARIFICATION 2026-09-04: Phase 4 mayor-lrbvo deploys the eBPF for its go/no-go gates via the MANUAL Phase-1 loader mayor-g6u8s with static fixture maps, NOT the Phase-5 DaemonSet mayor-9gr0n -- the DaemonSet is production-only and Phase 4 blocks it, so it cannot exist yet. See lrbvo notes for the full reasoning. Prevents mis-reading Phase 4 as requiring Phase 5 first.\nfiled 13 improvement/safety/investigation beads from the 2026-09-05 servicelb footprint review, aie31.8 through aie31.20. P1: aie31.8 map pinning, restart currently loses all flow state; aie31.9 REV_FLOW miss forwards a pod-source packet unencapsulated; aie31.10 POD_TARGETS holds one port per pod, blocking multi-port Services in Phase 5; aie31.11 flow-table admission control, the table is cyclable by 8192 unauthenticated packets. P2: aie31.12 disjointness doc fix, false for hostNetwork Pods; aie31.13 direct packet access, 7 helper calls per egress packet today; aie31.14 egress POD_TARGETS membership gate; aie31.15 conditional map writes; aie31.16 slim the resident loader; aie31.17 unified tagged flow table. P3: aie31.18 remap probe trims, aie31.19 DWARF strip, aie31.20 stateless Geneve MAC. Measured baseline via bpftool on a live kernel: FWD_FLOW 918400 plus REV_FLOW 983936 = 1902336 of the 1909072 sampler total; unified at 8192 entries = 983936, a 48 percent cut, with the tag byte free because BPF rounds key_size up to a multiple of 8. See bd memory ebpf-map-key-size-rounds-up-to-8.\nEPIC UNPARKED (operator 2026-09-07): all 4 design decisions resolved -- bguco (separate hooks sharing programs+maps), g3lag (tuple-based QUIC, CID deferred), aie31.11 (promote-on-bidi admission, map sizes configurable by the DaemonSet), aie31.21 (Option A stored full backend identity + scoped eviction). Implementation phases + design-improvement beads (aie31.11 admission, aie31.21 affinity+eviction, tavxy delivery-node validation) are now dispatchable; operator paces the build. Doc consolidation (axzsf) records the decisions into extended-context/ADRs, does the VIP rename, deletes the stale bguco findings doc. One open sub-question: dksf5 TCP-eviction yes/no.","status":"open","priority":2,"issue_type":"epic","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:45Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:45Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-xjy5o","title":"design(servicelb): CID-based QUIC keying needs cluster-wide server_id-to-endpoint distribution, not just a per-node map","description":"Operator raised 2026-09-07 on g3lag: the server-id-encoded CID (QUIC-LB draft) path the doc treats as a later opt-in has an un-spelled-out cross-node cost. Each node runs its own eBPF with its own map, so a per-node CID map cannot route a client that reconnects/migrates onto a DIFFERENT node. For server-id CID routing to work cluster-wide, EVERY node's eBPF needs (a) the shared CID-decode config (QUIC-LB encoding key/params, rotated in lockstep) and (b) a replicated server_id-to-backend-location table; a node lacking either cannot decode/route the CID. Operator's proposed mechanism (CONFIRMED correct + necessary): the cooperative L7 LB/backend allocates a persistent server_id per endpoint and publishes it via a CRD (or EndpointSlice/Pod annotation, or dedicated channel) that every node's userspace controller watches and programs into its eBPF map -- same distribution pattern as today's VIP/endpoint maps, keyed on server_id. UPSIDE worth recording: this scheme SOLVES cross-node reconnection/migration affinity that per-node conntrack FWD_FLOW pins fundamentally cannot (routing target travels in the CID, so any node decodes-and-routes statelessly; conntrack pins are node-local and break on cross-node reconnect). COST (reinforces g3lag's pass-through-now recommendation): threefold -- backend cooperation (only Envoy generates QUIC-LB CIDs today), shared+rotated decode secret across all nodes, and cluster-wide server_id-to-endpoint distribution. 4-tuple pass-through needs none of it, accepting only affinity-breaks-on-QUIC-migration. Coupling: same per-node convergence lag as 1dwwz drift, but smaller surface (per-endpoint-churn, not per-flow); tavxy delivery-node validation still bounds worst case to a drop. Action: fold into the g3lag findings doc's CID-based-later cost section; no code. Part of parked aie31 epic.\n","notes":"RESEARCH RESULT (2026-09-07): Envoy does NOT publish a discoverable server_id-per-endpoint CRD. server_id is STATIC config (config.core.v3.DataSource: inline/file/env var; AWS EKS's NLB controller injects AWS_LBC_QUIC_SERVER_ID as a per-pod env var Envoy reads at startup). No xDS, no k8s resource surfaces the mapping. QUIC-LB draft explicitly puts parameter distribution OUT OF SCOPE (out-of-band). Envoy Gateway / Istio / Gateway API do not expose server_id. IMPLICATION: u7s must OWN the server_id distribution -- cannot observe an existing Envoy resource. Model: (1) u7s assigns the server_id space to nodes/endpoints, (2) publishes its OWN CRD/ConfigMap that its per-node eBPF watches for server_id-\u003e{backend}, (3) injects the IDs into Envoy (or the NLB controller does, on AWS). Net: CID-based keying requires u7s to BUILD+OWN an assignment+distribution+injection subsystem -- MORE machinery than 'observe Envoy', further reinforcing g3lag's pass-through-now recommendation. Refs: envoyproxy.io quic_lb.proto; quicwg.org load-balancers draft; aws-load-balancer-controller QUIC guide.","status":"open","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:20Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:20Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-td3jz","title":"servicelb: verify CONFIG-map pin upgrade-safety (value size/layout mismatch across loader versions vs aie31.8 pinning invariant) + uplink_l2_header_len blanket non-Ethernet=0 default (both #1619-review suspicions -- confirm or guard)","description":"\n","status":"open","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:18Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:18Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-mmnr2","title":"operator: promote ebpf-build to a required merge-queue status check","description":"Outstanding operator-owned action (GitHub ruleset change — needs 2FA, not doable via gh CLI).\n\nebpf-build is the only CI job that compiles and tests the servicelb / servicelb-ebpf crates (they are excluded from the root workspace because aya does not compile on macOS). It is NOT in the main-branch merge-queue required-status-checks ruleset, so an eBPF-crate regression can still merge green.\n\nOrigin: mayor-de18e (closed) covered both \"add ebpf-build to the required ruleset\" and clippy/fmt for the servicelb crates. It was closed with only ebpf-memory-smoke made required; the ebpf-build half of its scope was not completed and had been living only on ai/dashboard.md. This bead re-tracks that dropped half so it survives a dashboard rewrite.\n\nInterim enforcement (why this is P3, not urgent): #1627 (mayor-7uyqb) added the servicelb cargo-test step to the ebpf-build job plus path-conditional local git hooks, and the mayor manually confirms ebpf-build green before merging any servicelb PR.\n\nDONE WHEN: ebpf-build is in the ruleset's required-status-checks set, so a red ebpf-build can never merge to main.\n\nMechanism for the operator: see memory ruleset-required-check-changes-need-operator-2fa-not-gh-cli.\n\n","status":"open","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:16Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-elmno","title":"test-fidelity: conformance harness leaves #1525 podIP-allowlist dormant (node-ipam disabled in lima; prod SAFE via ipam+Flannel) — DEFERRED P3, revisit with eBPF/Flannel-uniform work","description":"Surfaced live by mayor-ek41c (PR #1525). The whole elicb-\u003eek41c chain built a CIDR-allowlist (podIP in node.spec.podCIDR) to close the arbitrary-external control-plane-SSRF class for non-hostNetwork pods. BUT this repo's conformance KCM (scripts/conformance/04-start-kcm.sh) runs with -node-ipam-controller DISABLED, so node.spec.podCIDR is NEVER populated (confirmed live: empty podCIDR on a Ready node; ek41c's strict impl 502'd every pod /proxy). ek41c ships a fallback: non-hostNetwork podIP is accepted blocklist-only when the node has no podCIDR (= the pre-ek41c k6m4a ceiling), with the allowlist auto-activating once a real per-node CIDR exists. So TODAY ek41c delivers only the hostNetwork loopback-floor + CRLF fixes; the non-hostNetwork arbitrary-external SSRF remains open (bounded by node-compromise, same residual). INVESTIGATE: (1) WHY is node-ipam-controller disabled -- deliberate (u7s networking model does not use per-node podCIDR allocation?) or incidental? (2) Does u7s's actual pod networking (CNI/CRI-O setup) assign pods from a per-node CIDR that node.spec.podCIDR could reflect, or is podIP allocation node-agnostic? If node-agnostic, the CIDR-allowlist approach is FUNDAMENTALLY inapplicable and blocklist-only (+hostNetwork floor) is the ceiling for this networking model -- a significant scoping conclusion. (3) If per-node CIDR IS viable, is enabling -node-ipam-controller (+ --cluster-cidr/--allocate-node-cidrs) safe here, and does it then activate ek41c's allowlist (re-run ek41c's kubectl SSRF repro with a populated podCIDR)? Outcome: either enable ipam (closes the class) or document that blocklist-only is the ceiling and re-scope the SSRF residual accordingly. No code change in this bead -- investigation + verdict. VM (stack-only) to test enabling ipam.\n","notes":"OPERATOR DOWNSCOPE (2026-09-02): don't investigate open-endedly — check whether node-ipam-controller has a dependency straight-up incompatible with u7s; if not, enable it and call it a day.\n\nMAYOR PRE-DISPATCH FINDINGS (04-start-kcm.sh + lima-start.sh):\n- ipam disabled at 04-start-kcm.sh:158 (--controllers '...,-node-ipam-controller,...'); no --cluster-cidr/--allocate-node-cidrs/--node-cidr-mask-size anywhere. Premise confirmed.\n- u7s ALREADY uses per-node pod CIDRs, but assigned OUT-OF-BAND by lima-start.sh, NOT KCM: POD_SUBNET_OCTET (lima-start.sh:263-268) = suffix-1 (primary=0), pod subnet 10.85.\u003coctet\u003e.0/24, baked into CRI-O bridge CNI conflist (10-crio-bridge.conflist) + cni0 (line 303). Inter-node routes are static, no CNI/BGP (lima-start.sh:976).\n- =\u003e Enabling KCM ipam adds a SECOND independent allocator. Compatibility hinges on whether KCM-assigned node.spec.podCIDR MATCHES the CNI's real 10.85.X.0/24. Single-node default topology: --cluster-cidr=10.85.0.0/16 --node-cidr-mask-size=24 =\u003e KCM assigns 10.85.0.0/24 to sole node = CNI 10.85.0.0/24 (likely MATCH). Multi-node: KCM registration-order vs lima-start.sh suffix-order can DRIFT.\n- RISK: a MISMATCHED podCIDR is strictly worse than status quo — #1525's allowlist activates and REJECTS legit pods. Worker MUST verify the match live before shipping; ship only if it matches, else report incompatibility + revert + file follow-on for the deeper align-allocators fix.\n- Also verify KCM (controller-manager SA, not a node) writing spec.podCIDR is NOT blocked by mayor-elicb's NodeRestriction-equivalent admission (#1518).\n\nSEQUENCING: elmno's allowlist-activation check needs #1525's proxy code, not yet on main. Dispatch elmno off FRESH main AFTER #1525 merges.\nOPERATOR REFRAMING (2026-09-02) — corrects my earlier 'two competing allocators / drift risk' framing: lima-start.sh's per-node CIDR assignment is a STAND-IN for the disabled ipam controller, NOT a competing allocator. Design intent: enable node-ipam-controller =\u003e KCM becomes the SINGLE source of truth for node.spec.podCIDR; lima-start.sh should no longer COMPUTE the subnet. Drift concern dissolves (one allocator).\n\nIMPLEMENTATION NUANCE (makes this a moderate refactor, not a one-liner): CRI-O's stock bridge CNI reads a STATIC conflist (/etc/cni/net.d/10-crio-bridge.conflist) and does NOT natively consume node.spec.podCIDR. So 'script no longer needs to do it' concretely = lima-start.sh stops computing POD_SUBNET from node suffix (lima-start.sh:263-268) and instead READS KCM's assigned node.spec.podCIDR and writes THAT into the conflist. Two knock-ons: (1) BOOTSTRAP ORDERING — today the subnet is set at node bring-up; with ipam it must WAIT for KCM to assign podCIDR before pods can schedule; (2) INTER-NODE STATIC ROUTES (lima-start.sh:976,1006-1007, keyed off the suffix-computed POD_SUBNET) must switch to KCM's podCIDR.\n\nREVISED elmno scope (still behind #1525/npjfm):\n1. Enable node-ipam-controller in 04-start-kcm.sh (remove from --controllers disable list :158; add --allocate-node-cidrs=true --cluster-cidr=10.85.0.0/16 --node-cidr-mask-size=24 so KCM's scheme matches the existing 10.85.\u003coctet\u003e.0/24 addressing).\n2. Rework lima-start.sh: source the CNI conflist subnet + inter-node routes from KCM's node.spec.podCIDR instead of suffix arithmetic; handle wait-for-podCIDR ordering.\n3. Verify KCM (controller-manager SA) writing spec.podCIDR is NOT blocked by mayor-elicb's NodeRestriction admission (#1518).\n4. Verify end-to-end live (VM): node gets podCIDR from KCM -\u003e CNI uses it -\u003e real pods get IPs within it -\u003e #1525/npjfm allowlist ACTIVATES and legit non-hostNetwork pod proxy still works (re-run ek41c SSRF repro with a populated podCIDR). Multi-node optional but ideal.\nOPERATOR PRIORITY (2026-09-02): test-setup SIMPLICITY over architectural purity. If KCM-authoritative is complicated (KCM comes online AFTER the CIDR is assumed configured), prefer a simpler option.\n\nDECISION -\u003e go SIMPLE (supersedes the earlier 'KCM-authoritative, rework lima-start.sh' scope in the prior note):\n- Enable node-ipam-controller in 04-start-kcm.sh (remove from --controllers disable list :158; add --allocate-node-cidrs=true --cluster-cidr=10.85.0.0/16 --node-cidr-mask-size=24 so KCM's allocation MATCHES lima-start.sh's existing 10.85.\u003coctet\u003e.0/24 scheme).\n- LEAVE lima-start.sh's CNI subnet computation UNCHANGED (lima-start.sh:263-268). It keeps setting the CNI subnet at node bring-up as today -\u003e pods schedule immediately, NO wait-for-podCIDR, NO bootstrap reorder, NO inter-node-route rework. KCM merely populates the node.spec.podCIDR FIELD alongside; #1525/npjfm allowlist falls back to blocklist-only until the field appears, then activates.\n- Single-node (default conformance topology): KCM's podCIDR = lima-start.sh's CNI subnet = 10.85.0.0/24 by construction -\u003e allowlist activates, nothing to reconcile.\n- CAVEAT (document, don't fix unless required): multi-node runs can DRIFT (KCM registration-order vs lima-start.sh suffix-order); a mismatched podCIDR would make the allowlist reject legit cross-node pods. Only if multi-node conformance-under-allowlist becomes a hard requirement do we revisit the complex KCM-authoritative design (lima-start.sh reads podCIDR).\n\nWORKER VERIFY LIVE (VM, single-node): (1) KCM (controller-manager SA) writing spec.podCIDR is NOT blocked by mayor-elicb NodeRestriction (#1518); (2) node gets podCIDR 10.85.0.0/24 matching the CNI subnet; (3) #1525/npjfm allowlist ACTIVATES and legit non-hostNetwork pod proxy still works (re-run ek41c SSRF repro with populated podCIDR); (4) conformance smoke still green. Still queued behind #1525/npjfm merge.\nPREMISE CORRECTION (2026-09-02, mayor research of scripts/install.sh): the bead title/premise ('non-hostNetwork SSRF not actually closed') is WRONG FOR PRODUCTION. install.sh:998 ENABLES node-ipam-controller (--allocate-node-cidrs=true --cluster-cidr=10.244.0.0/16 --node-cidr-mask-size=24; -node-ipam-controller NOT in the disable list) and uses FLANNEL as the CNI (install.sh:669-678,1060+; disables the static crio-bridge, deploys manifests/flannel.yaml) which CONSUMES node.spec.podCIDR. So production = textbook upstream model: KCM = sole podCIDR allocator, Flannel = consumer. =\u003e node.spec.podCIDR IS populated in prod =\u003e #1525/#1527's podIP allowlist IS ACTIVE in prod =\u003e the non-hostNetwork control-plane SSRF class IS genuinely closed in real deployments.\n\nThe dormancy is CONFORMANCE-HARNESS-ONLY: lima-start.sh (963) uses CRI-O's stock static bridge CNI 'no CNI swap needed' for test speed, and 04-start-kcm.sh:158 disables node-ipam-controller, then lima-start.sh computes 10.85.\u003coctet\u003e.0/24 out-of-band. That out-of-band computation is the rogue 2nd allocator behind the multi-node drift.\n\n=\u003e elmno is RE-SCOPED from 'P1 close prod SSRF' to TEST-FIDELITY (make conformance exercise the active-allowlist path prod already runs, so allowlist regressions are caught). Lower priority. Production-faithful harness fix = enable ipam (single allocator) + make the harness CNI CONSUME podCIDR — either (i) adopt Flannel in lima-start.sh like prod, or (ii) make the static crio-bridge read podCIDR into its conflist. The earlier 'simple path' (align cluster-cidr, keep lima-start.sh computing) and 'deterministic write' are BOTH backwards from prod (harness stays an independent allocator) — reject. Running worker a8c0431f paused; simple-path PR NOT to be opened. Operator deciding: do the consume-model fix now vs defer (prod already safe).\nLIVE EVIDENCE from worker a8c0431f (2026-09-02, VM lima-node-4, since cleaned up — no PR, elmno left open):\n1. Enabling node-ipam-controller WORKS: KCM assigned podCIDR (range_allocator.go:433 'Set node PodCIDR podCIDRs=[10.85.0.0/24]'); kubectl confirms spec.podCIDR=10.85.0.0/24. mayor-elicb's NodeRestriction (#1518) does NOT block the controller-manager SA's write (no forbidden errors in /tmp/kcm.log).\n2. DRIFT IS A LIVE REGRESSION, worse than the mayor's earlier single-node-safe assumption: lima-start.sh's POD_SUBNET_OCTET = NODE_SUFFIX-1, so VM lima-node-4 (suffix -4) gets CNI subnet 10.85.3.0/24, while KCM always hands the sole registering node the cluster-cidr's FIRST /24 (10.85.0.0/24). A real pod got 10.85.3.7 and proxy was REJECTED: 'status.podIP 10.85.3.7 rejected: pod IP is not within node lima-node-4 podCIDR 10.85.0.0/24' — STRICTLY WORSE than status quo (blocklist-only currently allows it). The 'single-node-\u003eoctet 0-\u003ematch' assumption holds ONLY for the literal default VM name 'lima-node' (empty suffix); EVERY worker-isolation VM (and every multi-node topology) drifts. =\u003e the 'simple path' (align cluster-cidr + keep lima-start.sh computing) is DEAD.\n3. Consume-model refactor (KCM allocates + crio-bridge consumes podCIDR, mirroring prod's Flannel) is FEASIBLE but a real refactor: (a) bootstrap reorder — node registers, then lima-start.sh polls node.spec.podCIDR and writes it into 10-crio-bridge.conflist (not precomputed octet); (b) inter-node static routes (lima-start.sh ~976/1006) re-keyed off KCM's podCIDR; (c) gate pod scheduling until the conflist is written. This is the only correct fix and it matches production.\n\nCombined with the prod-is-already-safe correction: elmno is TEST-FIDELITY, and its only correct implementation is the consume-model refactor. Operator deciding do-now vs defer-as-P3.\nOPERATOR DECISION (2026-09-02): DEFER. Keep lima-start.sh's hardcoded per-node CIDRs as-is (a test-speed convenience, not a correctness necessity). Production is already correct (ipam+Flannel), so no urgency. Re-scoped P1-\u003eP3 TEST-FIDELITY. TRIGGER TO REVISIT: when the eBPF ServiceLB work lands (mayor-aie31 epic / mayor-g6u8s), evaluate enabling Flannel UNIFORMLY across environments (prod already uses it) — that would make the conformance harness consume podCIDR like prod, activating #1525/#1527's allowlist in tests and closing this test-fidelity gap as a natural rider, avoiding a standalone harness-networking refactor. Do NOT do the standalone consume-model refactor now. Narrow residual gap: conformance doesn't exercise the podIP-in-podCIDR membership branch under a populated podCIDR (unit-test-covered).","status":"open","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:13Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:13Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-d9o3d","title":"servicelb-ebpf: backend src-port remap re-derives every packet -\u003e LRU eviction reassigns port mid-connection","description":"From #1567 pa0ze round-3 critical review (MED, review 5115377623, confirmed by execution). resolve_backend_src_port (crates/servicelb/servicelb-common/src/lib.rs ~165) re-derives the synthetic backend src-port from the same deterministic seed on EVERY packet with no persisted decision. If an LRU eviction removes an UNRELATED occupant at an earlier probe index between two packets of the same flow, the flow re-probes, finds that earlier slot now free, and commits a DIFFERENT port mid-connection -\u003e reverse-path breakage. Only manifests under map-full LRU eviction pressure (conntrack maps are LRU_HASH). Robust fix: persist the flow's chosen synthetic port in its FWD_FLOW state and read it back on subsequent packets instead of re-deriving (approach (a) from the round-3 brief) -- idempotent by construction, no re-probe. Test under eviction: commit port_X, free an earlier-probe-index occupant, assert the next packet still resolves to port_X. RELATED: mayor-lrbvo (Phase-4 real-fleet churn/eviction + RSS measurement) is the natural home to validate this under real pressure; the PR already defers general churn/eviction there. This is a narrower, specific instance of that deferral, tracked so it is not lost.\n","notes":"BLOCKED on aie31.17 (operator decision 2026-09-08). Fix-round proved node-locality makes clean persistence impossible without the unified-flow-table redesign: backend_src_port is decided+re-read on the BACKEND node (try_geneve_decap_forward), FWD_MAIN/FWD_PENDING are ingress-node-only, BPF maps are per-node. Round-1 (#1628, closed unmerged) added a 2nd REV_FLOW key under (VIP,client) front_key -\u003e finding-2 collision risk (VIP/pod-CIDR NOT disjoint for hostNetwork Pods, per aie31.17) + doubled occupancy. Resolve as part of / after aie31.17's explicit-tag unified table.","status":"open","priority":3,"issue_type":"bug","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:12Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:12Z","dependencies":[{"issue_id":"mayor-d9o3d","depends_on_id":"mayor-aie31.17","type":"blocks","created_at":"2026-09-09T00:41:30Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-axzsf","title":"docs+code(servicelb): rename misleading VIP - the LB front IP is not necessarily virtual, no cloud-LB assumption","description":"Operator note 2026-09-07: the eBPF ServiceLB docs and code call the external service front IP a client dials the VIP. In u7s that IP is NOT necessarily virtual: u7s itself IS the cluster north-south LoadBalancer and must never assume the presence or absence of a cloud LB in front of the cluster, so the front IP may be a real routable IP, not a virtual one. Rename to an accurate neutral term across the surface. Proposed term: LB front IP (map VIP_MAP to LB_FRONT_MAP, VipBackend accordingly) - final term to confirm. Also state the no-cloud-LB-assumption principle explicitly in the mechanism doc. Scope: ai/extended-context/ebpf-lb-dataplane.md, docs/decisions/servicelb-ebpf-geneve-dataplane.md and siblings, and code comments/identifiers in crates/servicelb. DEFERRED doc-hygiene under the aie31 epic (currently parked); coordinate so it does not collide with in-flight aie31 work. Fold in recording the settled aie31 design decisions into the mechanism doc while touching it.\n","notes":"Docs-done via PR #1609 (2026-09-07): recorded the 4 settled aie31 decisions\n(bguco hook topology, g3lag/xjy5o QUIC keying, aie31.11/aie31.21 admission+\naffinity split into a new ADR docs/decisions/servicelb-flow-admission-affinity.md,\ntavxy drift invariant) in ai/extended-context/ebpf-lb-dataplane.md; renamed\n\"VIP\" to \"LB front IP\" in prose across ebpf-lb-dataplane.md and the two\nservicelb decision ADRs; deleted the closed-bead findings doc\nai/findings/2026-09-06-mayor-bguco-tc-bpf-hook-topology.md.\n\nREMAINING (keep this bead OPEN for): the CODE rename in crates/servicelb\n(VIP_MAP -\u003e LB_FRONT_MAP, VipBackend/VipKey identifiers, doc comments in\nmain.rs) + any follow-on implementation. Doc prose is done; code identifiers\nwere deliberately left untouched per the docs-only scope of PR #1609.\nAlso delete (hygiene 2026-09-07): ai/findings/2026-09-06-mayor-g3lag-quic-cid-conntrack-keying.md -- g3lag is now CLOSED (tuple-based decision), so per the findings lifecycle this doc is stale and the hygiene loop flags it hourly. Its DECISION is already recorded in extended-context + the servicelb ADRs (#1609); the QUIC-LB proxy survey detail is preserved in git history for xjy5o's future CID revisit. Bundle this deletion with axzsf's remaining eBPF-surface work (the VIP_MAP/VipBackend/VipKey code rename), so no standalone CI cycle is spent on a doc deletion.\nFindings-doc cleanup now covers MULTIPLE closed aie31 beads (hygiene flags them hourly): delete ai/findings/2026-09-06-mayor-g3lag-quic-cid-conntrack-keying.md AND ai/findings/2026-09-06-mayor-aie31.11-flow-table-admission-control.md (aie31.11 closed via #1611; decision recorded in the ADR/extended-context, analysis in git history). Generalize: when axzsf's eBPF-surface PR runs, sweep ai/findings/ for any 2026-09-06-mayor-aie31.* / eBPF findings doc whose bead is CLOSED and delete it (git history is the archive). This batches all of them into one eBPF-surface commit rather than a CI cycle per doc.","status":"in_progress","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:11Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:51Z","started_at":"2026-09-08T15:40:51Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.5","title":"servicelb-ebpf: uplink parse hard-codes Ethernet L2 (ETH_HLEN=14) -- silently no-ops on L3-only uplinks (WireGuard/overlay)","description":"WireGuard-over-Lima spike (2026-09-04, lrbvo) found: uplink_ingress/uplink_egress_return in crates/servicelb/servicelb-ebpf/src/main.rs hard-code ETH_HLEN=14 and read the EtherType at ctx.load::\u003cu16\u003e(12), then index the IPv4 header at ETH_HLEN-relative offsets. A WireGuard (or any L3-only/tun) uplink has NO 14-byte Ethernet header, so the EtherType load reads into the middle of the real IP header, never matches 0x0800, and the program falls through to TC_ACT_OK on every packet -- the dataplane SILENTLY no-ops on an L3 uplink (no crash). Blocks Phase-4 tier-1 (Lima/VPS over WireGuard) and any overlay/L3 uplink. FIX: make the header-skip conditional (0 for L3-only, 14 for Ethernet) -- e.g. a load-time flag from the loader (it knows the iface type) or runtime detection. The tc-bpf ATTACH to an L3/no-L2 wg iface ALREADY WORKS (spike proved via bpftool); this is purely the parse-offset assumption. SHARED SURFACE with pa0ze/#1567 (servicelb-ebpf) -- dispatch AFTER #1567 merges. Verify via the smoke harness (attach + round-trip) on a wg uplink.\n","status":"closed","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:07Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:56Z","closed_at":"2026-09-08T15:40:56Z","close_reason":"PR #1619: L2-header-skip fix landed with fail-on-revert unit tests, verified against real WireGuard traffic (FWD_PENDING correctly classified) and single-node L2 smoke.sh unchanged","dependencies":[{"issue_id":"mayor-aie31.5","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:01Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.3","title":"docs(ebpf-lb): correct map type LRU_PERCPU_HASH -\u003e LRU_HASH in ebpf-lb-dataplane.md (pa0ze empirical finding)","description":"pa0ze (#1567) empirically found the design doc's mandated BPF_MAP_TYPE_LRU_PERCPU_HASH breaks conntrack: forward-write and return-read are different packets with no same-CPU affinity, so per-CPU maps keep separate per-CPU values and the return read misses the forward write (confirmed via bpftool -- a SYN retransmit landed on a different per-CPU slot; LRU_HASH passed 4/4). Code now uses LRU_HASH (bounded, same 8192 ceiling). Correct ai/extended-context/ebpf-lb-dataplane.md's Conntrack section (map-type spec + any per-CPU rationale) to match, referencing bd memory servicelb-lru-percpu-hash-breaks-conntrack. Small Shape-6 doc fix; land after #1567 merges; keep within the doc word cap.\n","notes":"ADJACENT to the map-type fix: the doc's footprint table (~4-7 MiB 'per-CPU' for the conntrack maps) is now DOUBLY stale -- LRU_HASH is SHARED (single table), NOT per-CPU, so the real footprint is ~one 8192-entry TCP table (~0.5 MiB) + QUIC (~0.25 MiB), NOT multiplied by vCPU count. Correct the footprint numbers alongside the LRU_PERCPU_HASH-\u003eLRU_HASH map-type correction in ebpf-lb-dataplane.md.","status":"closed","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:05Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:57Z","closed_at":"2026-09-08T15:40:57Z","close_reason":"PR #1614 opened","dependencies":[{"issue_id":"mayor-aie31.3","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:00Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.23","title":"chore(ci): re-tighten eBPF map-memory ceiling 4 MiB -\u003e ~2 MiB after map unification","description":"After the flow tables are unified (aie31.17), re-tighten the eBPF map-memory\nCI tripwire from 4 MiB to ~2 MiB, so the guard stays proportional to the real\nfootprint and locks in the unification win against a silent regression (an\naccidental un-unify, a doubled entry count).\n\nNumber and rationale. Unified at 8192 entries is 983,936 bytes plus ~6,736\nfor the small maps (CONFIG, VIP_MAP, POD_TARGETS) = ~0.945 MiB. A 2 MiB\nceiling preserves the tripwire's current ~2.1x-headroom policy (today's 4 MiB\nover the 1.82 MiB two-map footprint is the same ratio), still catches a gross\nblow-up such as a 8192 to 81920 typo, and survives QUIC folded into an\n8192-entry unified table.\n\nDo NOT hardcode 1 MiB. It leaves ~5% headroom over the post-unification\nfootprint, abandons the deliberate tripwire-not-budget policy in\nscripts/servicelb/assert-ebpf-map-memory.sh, and re-creates the exact\ntoo-close-to-reality fragility that merged #1568 red (a 1 MiB ceiling failing\non the real 1.82 MiB Phase-3 footprint, fixed by raising to 4 MiB in #1569).\n\nRevisit the number if aie31.17's entry-count decision raises the ceiling\nabove 8192, or when native QUIC lands with its own sizing -- the doc\noriginally budgeted TCP 8192 plus QUIC 4096, which as a unified 12288-16384\ntable would be 1.4-1.9 MiB and could crowd a 2 MiB ceiling. The entry-count\ndecision is itself downstream of admission control (aie31.11), so confirm the\nsettled footprint before editing the limit.\n\nMechanical change: the limit line in scripts/servicelb/assert-ebpf-map-memory.sh\n(limit=$((4 * 1024 * 1024))) plus its explanatory comment, and the ~1.82 MiB\nfootprint figure in that comment.\n\nBlocked by aie31.17 (unification must land first, or the guard trips on the\ncurrent two-map footprint).\n\nDONE WHEN: the CI ceiling is 2 MiB, the assert script's comment states the\npost-unification footprint and the 2x-headroom rationale, and ebpf-memory-smoke\npasses green on the unified dataplane.\n\n","status":"open","priority":3,"issue_type":"chore","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:03Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:03Z","dependencies":[{"issue_id":"mayor-aie31.23","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:12Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.23","depends_on_id":"mayor-aie31.17","type":"blocks","created_at":"2026-09-09T00:41:29Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.22","title":"feat(servicelb): expose flow-table traffic-pattern metrics as observability (not a resize control loop)","description":"Expose flow-table traffic-pattern metrics from the dataplane as observability\nthe loader can read and publish, so operators can right-size the fixed map\nceiling and get alarmed when a node runs hot -- explicitly NOT to drive a\nruntime resize control loop (see aie31.11 for why that is declined).\n\nWhy observability, not control. A BPF map has no live element count\n(confirmed: bpf_map_info carries max_entries but no current-count field), and\nLRU eviction is silent, so an accurate live occupancy for an LRU map is\nexpensive to obtain (full-map iteration) or drifts (a self-maintained counter\ncannot be decremented on eviction). What is cheap to expose is a small set of\npressure proxies the datapath can maintain in a PERCPU_ARRAY counter map:\n\n- new-flow inserts per interval (forward-path insert of a previously-absent\n  key)\n- churn: inserts of a key that was recently evicted, the real \"table too\n  small\" signal\n- optionally, high-water concurrent-flow estimate\n\nThe loader samples the counter map periodically -- the same shape as the\nexisting scripts/servicelb/sample-ebpf-memory.sh sampler -- and publishes it\nalongside the map-memory and loader-RSS CSVs.\n\nUses:\n1. Right-size the fixed max_entries per node at the NEXT load, feeding the\n   load-time configurability knob rather than a reactive swap.\n2. Alarm when churn indicates a node is thrashing its conntrack table, which\n   is the legitimate-traffic face of the admission-control concern in\n   aie31.11 (a burst of new flows evicting established ones).\n\nCost caveat: the churn proxy adds a compare on the hot path, which cuts\nagainst the direct-packet-access work in aie31.13. Measure before committing\nto the churn signal specifically; the plain new-flow counter is nearly free.\n\nDONE WHEN: the loader publishes at least a new-flow-rate metric sampled from a\ndatapath counter map, in the same CSV/observability shape as the existing\nsampler, with the hot-path cost measured.\n\n","status":"open","priority":3,"issue_type":"feature","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:02Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:02Z","dependencies":[{"issue_id":"mayor-aie31.22","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:11Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.22","depends_on_id":"mayor-aie31.11","type":"relates-to","created_at":"2026-09-09T00:41:37Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.20","title":"design(servicelb): stateless return-path authorization via a Geneve MAC","description":"Investigate replacing FWD_FLOW entirely with a keyed hash carried in the\nGeneve option, since that map stores nothing anyone reads.\n\nFWD_FLOW's value is written at servicelb-ebpf/src/main.rs:238 and never read.\nThe only consumer, at line 496, discards it and checks existence alone. So it\nis not an affinity table at all, it is a return-path authorization table\nanswering exactly one question: did this node forward this flow? A question\nof that shape does not need storage.\n\nSketch: the ingress node stamps a keyed hash of the flow tuple into the\nforward Geneve option, the backend echoes it alongside the VIP echo it\nalready sends, and the ingress verifies it against the reconstructed tuple on\nreturn. If it holds up, the larger of the two conntrack maps disappears along\nwith its eviction behaviour and its flush-attack surface.\n\nOpen questions that decide it:\n\n- Needs a per-node secret plus a coarse epoch in the MAC input to bound\n  replay of a captured return.\n- No HMAC helper exists in tc-bpf, so it means hand-rolling something like\n  SipHash-2-4. Cost is roughly comparable to the hash-map lookup it replaces.\n- Value depends on the underlay. Over a WireGuard or Tailscale mesh the\n  return path is already authenticated and the MAC adds little; in the plain\n  colocated case it is doing real work. All four underlays are in scope, so\n  it has to be justified against the weakest one.\n\nDepends on the admission-control decision, which may change what the forward\ndirection needs to store, and interacts with the unified-table bead, which\nassumes both populations still exist.\n\nDONE WHEN: a written go or no-go exists, with the underlay reasoning recorded.\n\n","notes":"SUPERSEDE/RE-SCOPE (operator decision on aie31.21, 2026-09-07): Option A keeps FWD_FLOW as the per-flow affinity store, so aie31.20's premise (FWD_FLOW stores nothing anyone reads -\u003e eliminate it) is now FALSE. Map-elimination is DEAD. Re-scope aie31.20 to 'return-path authorization via a stateless Geneve MAC' ONLY (replacing the main.rs:514 existence-check), keeping the map for affinity -- or supersede if not pursued. Not MVP-blocking.","status":"open","priority":3,"issue_type":"decision","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:00Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:00Z","dependencies":[{"issue_id":"mayor-aie31.20","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:10Z","created_by":"Erdos Balint","metadata":"{}"},{"issue_id":"mayor-aie31.20","depends_on_id":"mayor-aie31.11","type":"blocks","created_at":"2026-09-09T00:41:28Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.19","title":"build(servicelb): strip DWARF from the embedded eBPF object","description":"Strip DWARF from the eBPF object before it is embedded, keeping BTF.\n\nThe built object is 214,496 bytes and carries .debug_str, .debug_ranges,\n.debug_loc, and .debug_abbrev, confirmed by inspecting the artifact.\ncrates/servicelb/Cargo.toml sets debug 2 and strip false on the\nu7s-servicelb-ebpf package, which bpf-linker needs in order to generate BTF,\nbut the resulting DWARF is not needed at runtime. It is embedded verbatim\ninto the loader binary by include_bytes_aligned at src/main.rs:153 and read\nback at load.\n\nMust keep .BTF and .BTF.ext, which aya needs for map and relocation info.\nStripping happens after the bpf-linker step, so it likely belongs in build.rs\nalongside the aya-build invocation.\n\nDONE WHEN: the embedded object shrinks with BTF intact, and the loader still\nloads and attaches on the smoke fixture.\n\n","status":"open","priority":3,"issue_type":"chore","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:59Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:59Z","dependencies":[{"issue_id":"mayor-aie31.19","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:09Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.18","title":"perf(servicelb): trim the backend port-remap probe path","description":"Three contained cleanups in the backend port-remap path, all in the same\ncode, all covered by the existing common-crate tests.\n\n1. Compare occupant identity as raw scalars. servicelb-ebpf/src/main.rs:367\n   and :387 map each occupant through ipv4_mapped_v6 purely so it can be\n   compared. The mapping is injective, so comparing the raw address, port,\n   and client port is exactly equivalent and turns a 20-byte comparison into\n   an 8-byte one, once per probe iteration.\n\n2. Drop the dead high-half XOR in synthetic_port_seed,\n   servicelb-common/src/lib.rs:214. It mixes in front_ip bytes 0 through 4,\n   which are always zero for an IPv4-mapped address by construction, so the\n   term contributes nothing.\n\n3. Hoist the candidate key build out of the probe loop,\n   servicelb-ebpf/src/main.rs:384. Only two bytes change per candidate, but\n   the buffer's address escapes into bpf_map_lookup_elem, so the compiler\n   cannot hoist it and must re-materialize all 37 bytes each iteration. With\n   a constant trip count of 16 the loop very likely fully unrolls, putting 16\n   copies of a 37-byte stack build into program text.\n\nCalibration: steady-state per-packet cost here is low, because a remapped\nflow re-finds its own committed port on the first probe. Treat this as\nprogram size and stack headroom rather than throughput. The merged\ngeneve_ingress already carries several 37-byte key buffers against a 512-byte\nBPF stack limit.\n\nDONE WHEN: the common-crate tests still pass and the compiled program's\ninstruction count drops.\n\n","status":"open","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:58Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:58Z","dependencies":[{"issue_id":"mayor-aie31.18","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:09Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-88n36","title":"net/servicelb: measure real Envoy/nginx/HAProxy RSS on target 1GB/1vCPU node before L7-tier proxy choice","description":"The L7 TLS source-transparency finding (ai/findings/2026-09-03-l7-tls-source-transparency.md) cites Envoy ~150MB and nginx ~50-100MB baseline RSS from operational memory, uncited to any measurement. The ServiceLB ADR's own loxilb precedent (docs/decisions/servicelb-ebpf-geneve-dataplane.md) shows vendor/folklore memory figures can be wrong by a large margin on a real 1GB/1vCPU node (loxilb crash-loops on ENOMEM despite being expected to fit). If an L7 tier is ever prototyped, measure actual idle RSS for each proxy candidate (Envoy, nginx OSS + NGINX Gateway Fabric, HAProxy) on the same target hardware class before finalizing a choice, the same way Phase 4's gates (mayor-lrbvo) require measured RSS over the eBPF dataplane's own estimate.\n\n## Context\nSurfaced during verification pass on mayor-98i0y (PR: docs(findings) verify+refine L7 TLS source-transparency).\n","notes":"Gated on an L7 tier being prototyped; not scheduled until then.\nDesk-research findings: ai/findings/2026-09-06-mayor-88n36-l7-proxy-rss-baselines.md (pushed on worker/agent-addeeafb1e0cb97dc, no PR). Best CITED baselines: Envoy Gateway default 512Mi memory request (envoyproxy/gateway) = half a 1GB node; Istio-measured Envoy sidecar ~60MB (config-dependent); vendor-default requests ingress-nginx 90Mi, HAProxy ingress 400Mi, NGF none. Folklore (Envoy ~150MB / nginx ~50-100MB / HAProxy lightest) NOT substantiated as measured idle RSS; 'HAProxy lightest' refuted at controller-pod level (400Mi \u003e nginx 90Mi). Live on-hardware measurement still pending an L7 prototype + a Lima VM; doc ships a runnable 1GiB/1vCPU methodology. Awaiting operator.\n\n## critical-reviewer findings — findings-doc — mayor-88n36\n\n**Verdict**: needs-changes\n\n**Confirmed findings** (must be true, evidence cited):\n- [HIGH] Doc's own cached HAProxy source is mislabeled and its real figure — worse than everything else in the doc — is dropped from the table and the Answer. Evidence: `temp/research/haproxy-ingress-deploy.yaml:168-172` shows `requests: memory: 2048Mi` / `limits: memory: 2560Mi` for the manifest the haproxytech/kubernetes-ingress README calls \"Basic setup\" (`kubectl apply -f deploy/haproxy-ingress.yaml`, confirmed live on default branch); doc's References section instead describes this same cached file as \"(no resources set)\". Suggested fix: add a second HAProxy vendor-default row (2048Mi, this source) and correct the Answer — HAProxy's own quickstart manifest, not Envoy Gateway's 512Mi, is the most alarming cited figure, and it alone exceeds the entire 1GiB target node.\n- [LOW] ADR citation is off by a line: doc cites `docs/decisions/servicelb-ebpf-geneve-dataplane.md:35` for the \"ENOMEM crash-loop\" detail, but line 35 only reads \"...client address across nodes. loxilb, a real eBPF\" — the ENOMEM/2GiB text is lines 36-38 (which the doc's own second citation, two paragraphs later, correctly points to). Evidence: `docs/decisions/servicelb-ebpf-geneve-dataplane.md:35-38`. Suggested fix: widen the first citation to `:35-38`.\n\n**Suspicions** (worth the maintainer's attention, but I did not confirm):\n- none\n\n**Not reviewed** (out of scope for this deliverable's checklist):\n- Live on-hardware RSS measurement (doc explicitly performs none, methodology only).\n- Kernel-level precision of the cgroup `memory.peak`/`RssAnon` semantics described in the methodology (plausible on reading, not checked against kernel docs).\n\n**Meta**: reviewed at 2026-09-06T10:14Z, deliverable size (1 file / 209 LoC / 1 bead), checklist items checked (5/5).","status":"open","priority":3,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:47Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:47Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.4","title":"eventual: split servicelb eBPF to its own repo for fully independent dev/release (once feature-complete)","description":"Operator idea 2026-09-04. Once the servicelb eBPF library reaches feature-completeness (deliberately a limited feature set), split it into a SEPARATE repo so its development + release processes are fully independent of u7s -- versioned artifacts u7s consumes as a dependency. TRIGGER: feature-completeness, NOT before -- a premature split adds cross-repo friction while the dataplane API is still churning (Phases 3-5 are actively reshaping it). DEFERRED: revisit when Phase 4 (lrbvo) + Phase 5 (9gr0n) land and the surface stabilizes. Related open question -- deploying the eBPF on u7s as the Phase-5 DaemonSet needs a built container image pushed to a registry (see mayor-9gr0n note); that registry decision is needed for Phase 5 REGARDLESS of the split.\n","status":"open","priority":4,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:40:06Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:40:06Z","dependencies":[{"issue_id":"mayor-aie31.4","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:43:00Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"mayor-aie31.2","title":"net/servicelb: smoke-harness polish -- set -e diagnostic ordering + strip PR-ref comments (aie31.1 #1565 review LOWs)","description":"Non-blocking LOWs from the #1565 critical review (5113708051), harness fail-closed already confirmed. (1) smoke-remote.sh:128 and :141 -- a var=$(failing-pipeline) assignment trips set -e before the explicit FAIL-message check runs, so the friendly diagnostic is skipped and in the verifier-accept case VERIFIER-ACCEPT: PASS is echoed to the log BEFORE the check that can fail. Reorder so the PASS line is only emitted after the check passes, and the FAIL diagnostic actually prints. (2) smoke-remote.sh:6 and :91 embed PR #1557 references in comments -- strip them (Rule 10: no PR refs in source, they rot). Scripts-only, no VM needed to verify beyond shellcheck + a dry re-run.\n","status":"open","priority":4,"issue_type":"task","owner":"valerauko@gmail.com","created_at":"2026-09-08T15:39:59Z","created_by":"Erdos Balint","updated_at":"2026-09-08T15:39:59Z","dependencies":[{"issue_id":"mayor-aie31.2","depends_on_id":"mayor-aie31","type":"parent-child","created_at":"2026-09-09T00:42:59Z","created_by":"Erdos Balint","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
